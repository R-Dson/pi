# Upstream Integration Ledger

Fork-specific integration bookkeeping. Update this file whenever fork work touches files that upstream also owns.

## Purpose

This fork evolves pi's session layer toward incremental append-only session processing with crash-safe recovery, a canonical tool pipeline, and bounded tool outputs, while keeping the upstream-visible patch surface small so merges from upstream `main` stay mechanical and easy. Everything outside the fork's own modules is treated as upstream code and changed only through entries in the table below, within the three change kinds and the move bar of the patch-surface policy row in the Decisions Log (2026-09-20).

## Decisions Log

Pre-made architectural decisions, with one-line rationales. Do not revisit without updating this ledger.

| Decision | Rationale |
| --- | --- |
| Projection cache skipped | `buildSessionContext` runs only on resume, session switch, and compaction — never per turn — because the agent mutates `state.messages` in memory. No hot path, so no cache. |
| Perf/token/cache baselines deferred | Tests must not use real providers or keys, and the faux provider cannot measure provider cache tokens, so there is nothing trustworthy to baseline against yet. |
| Upstream behavior kept: session file creation deferred until first assistant message | Forcing a flush on user-message commit would create session files for abandoned prompts. |
| Neutral directory naming: new session modules live in `core/sessions/` | Upstream-contribution friendly; does not advertise fork ownership in the path. |
| Incremental-index/projection-cache work folded into session validation | No serialized format changes; file order is the sequence, so correctness is validated before any indexing is added. |
| Full `ExecutionScope` (defer/dispose cleanup registry) skipped | Tools already receive an abort signal and clean up on it; a deferred-cleanup registry has no consumer today. Per-tool `timeoutMs` covers the deadline case. |
| Tool output bounding via coding-agent `afterToolCall` hook (not an agent-loop change) | `AfterToolCallResult.content` fully replaces the finalized result content in `finalizeExecutedToolCall` (packages/agent/src/agent-loop.ts), so bounding needs zero `packages/agent` diff. |
| Golden prompt tests are the phase-7 equivalence baseline (issue #12) | `packages/coding-agent/test/system-prompt.test.ts` (golden describe) and `packages/coding-agent/test/suite/prompt-stable-prefix.test.ts` pin today's system-prompt bytes and per-turn request-prefix stability (systemPrompt, tool order, extend-only messages) with zero src changes. No upstream src files changed. |
| Prompt-section framework and tool-order/schema normalization descoped (issue #12) | The baseline tests prove current assembly is already deterministic and prefix-stable within a session, across turns, and across sessions with identical state — the plan's goal (cache-stable deterministic construction) holds without restructuring. A section framework would add abstraction with no observable benefit. Revisit only if a provider-conversion instability surfaces; the tests are the equivalence gate for any such change. |
| Upstream `build-binaries.yml` disabled on the fork | Fork releases go through `fork-release.yml` (GitHub Packages + standalone tarball + prebuilt binaries, compiled by invoking the upstream `scripts/build-binaries.sh` with skip flags); upstream's tag-triggered npm-publish/binary machinery cannot work here (no OIDC/npmjs); the `v*` push trigger is removed in-file (manual dispatch remains), and fork-release tags are pushed by `github.token`, which never triggers workflows anyway |
| Permission enforcement ships only as the permission-policies extension; core `tools.permissions` policy mode removed (issue #70, supersedes the #15/#16 core mode) | Upstream's stated philosophy puts permission flows in extensions ("No permission popups... build your own confirmation flow with extensions"), the extension's interactive `ask` dialog is something core mode could not offer, and the core wiring sat in agent-session.ts and settings-manager.ts, the files that conflict most on syncs. Settings carrying `tools.permissions` keys load and are ignored like unknown keys; v0.84.3-fork.3 carried the mode, so the removal is a recorded breaking change. |
| Evaluator semantics: default effect `allow`, deny > ask > allow, user rules beat profile presets via layered evaluation, `hide: true` deny rules drop tools from the active list | Carried over from the core mode, now consumed by the extension: unmatched calls proceed (enabling policy with no rules must not break every session), layered evaluation keeps deny > ask > allow regardless of list order while any matching user rule wins, and hiding runs through the public `setActiveTools` path, so a stale model call fails in the agent loop with a terminal "Tool not found" error. |
| Interrupted-turn recovery appends ordinary toolResult message entries at resume, only where appending keeps each result adjacent to its tool_use (issue #84) | Plan 2.1 forbids new serialized entry types; appending one terminal error toolResult per dangling final-turn call repairs the turn for strict providers (Anthropic rejects tool_use without tool_result) without rewriting history. Anthropic also rejects tool_use/tool_result pairs separated by another message, so repair runs only when the dangling turn is the context's last message-bearing region (non-message entries after it are fine; persisted sibling toolResults count as adjacent). A mid-path dangling turn (e.g. a steering user message persisted mid-tool-run before the crash) resumes unrepaired — true adjacency would require rewriting history, which the design forbids — and such sessions remain unloadable-as-is by strict providers; the validator still flags the turn. |
| Fork publishes rename `@earendil-works/*` to `@r-dson/*` at publish time only (`scripts/fork-publish-github.mjs` stages and rewrites; repo manifests untouched) | Repo manifests stay upstream-identical, so upstream merges never conflict on package names or versions. |
| `npm-shrinkwrap.json` and `install-lock` excluded from fork publishes | The shrinkwrap pins `@earendil-works/*` names that do not exist on npm.pkg.github.com (npm forces it into tarballs); dependency integrity comes from exact version pins for direct deps plus the carried `overrides` for the known-bad transitives, with full transitive resolution left to npm (issue #78 tightened the claim). |
| Standalone install channel derives its dependency manifest mechanically from coding-agent's (`scripts/fork-standalone-package.mjs` drops `@earendil-works/*` entries, copies the rest verbatim, and carries `overrides`) | No curated dependency list to keep in sync; the release bundle already inlines all workspace packages, so only public-npm dependencies remain. `overrides` pin the known-bad transitives the excluded shrinkwrap would otherwise cover; their keys reference external packages only (`protobufjs`, `rimraf`, `gaxios.rimraf`), so they need no scope rewrite — carried verbatim like the registry manifests (issue #78). |
| Fused edit/write verification ships in core as the `thenRun` parameter, not as an extension (design: NVlabs/SoL-Pi Action Fusion, implemented natively; no SoL-Pi code imported) | An extension cannot run its fused command inside `withFileMutationQueue`, so fused writes would race plain edit/write calls from the same agent (SoL-Pi's external mutex and sha256 TOCTOU guard exist only to paper over that). In core the command runs in the mutation-queue slot and the guard is unnecessary. The `[thenRun:succeeded/failed/skipped]` markers are part of the tool result contract (exported constants in `core/tools/then-run.ts`), which extensions must not invent into core tool output. Because the fused command executes bash inside the mutation tool, the `tool_call` gate sees only the edit/write facet: the permission-policies extension additionally evaluates a `bash`/`process.execute` facet with `thenRun.command` for any call carrying `thenRun` and keeps the stricter outcome, so shell-denying policies still constrain fused commands. |
| Shell full-output spill moved from the OS temp dir to the session artifacts directory (design: NVlabs/SoL-Pi ObservationPack archive; path-as-handle, recall via the `read` tool) | Same directory the loop-level output bound already spills to, so retention semantics stay uniform across both bounds; the temp dir could vanish mid-session, killing recall of a truncated output's head. Content-addressed handles and a dedicated paging tool are deliberately not built — `read`'s offset/limit pages the spill file — and become worth adding only with a projection extension that needs stable ids. |
| Compaction-settings extension accessor deferred | The OCC-style boundary-compaction extension (SoL-Pi research) is the only consumer of effective `reserveTokens`/`keepRecentTokens` via `ExtensionContext`; it does not exist yet, so publishing the accessor now would be speculative API on upstream-owned files. `cacheWriteReadRatio` in pi-ai landed because it carries real logic; the accessor lands with its consumer. |
| Hidden thinking renders a live latest-run preview while streaming and a one-line `Thought for Ns` marker when finished (issue #30) | Hidden thinking previously showed only a static `Thinking...` label per run; showing the newest run's tail (the last six visual lines, fixed-height while streaming, per the assistant-message row below) signals liveness while transcripts stay compact. Display-only: no request, session, or settings changes; full text stays reachable via the existing `app.thinking.toggle` expansion. |
| Runtime traffic policy: user-configured provider APIs (provider-required headers only) + explicit user actions; zero pi.dev requests, zero telemetry pings (issue #32) | The fork phones home for nothing: install/version pings, the pi.dev catalog overlay, app-identification attribution headers, automatic startup update checks, and the analytics/tracking settings are all deleted rather than flagged off. Verification: `packages/coding-agent/test/suite/no-tracking-traffic.test.ts` asserts a representative session performs zero fetches. 2026-09-05 amendment: the two revivable behaviors returned as strictly opt-in settings (see the next row); the purge of install pings, the remote catalog, and automatic extension checks stands. |
| Fork self-update installs from the fork's GitHub release tarball or the fork registry; upstream-npm installs print a notice and still update (issues #29/#74) | The running install's package name identifies the channel: `@r-dson/pi-standalone` updates from `releases/latest/download/pi-fork.tgz` and any other `@r-dson/*` name updates from `@r-dson/pi-coding-agent@latest` (the user's .npmrc scope mapping resolves npm.pkg.github.com; neither may touch npmjs — `@earendil-works/pi-coding-agent@latest` would replace the fork with upstream), and an upstream npmjs install is told it tracks upstream with the install.sh one-liner to switch. No version check or banner is reintroduced; the update command runs only when the user asks. The tarball install appends `--allow-remote=all` only on npm >= 12 (older npm rejects the unknown option; see the config.ts row). |
| Compaction/branch-summary summarizer calls replay the byte-exact prior request prefix plus one appended instruction turn (issue #40; DSH-derived) | The old standalone summarizer request (serialized `<conversation>` blob, dedicated system prompt, no tools) was a full cache miss on the entire context at the moment it is largest; replaying the agent's real system prompt, full tool list (unfiltered), and converted history lets the provider prefix cache cover the call. The summarizer persona, checkpoint format instructions (format body byte-identical), and `customInstructions` fold into the appended user turn; a `previousSummary` switches that turn to the update prompt while the old summary itself is replayed as the checkpoint message heading the conversation (byte-identical projection, so the prefix matches from message 0). The split-turn turn-prefix summary keeps the standalone serialized shape (it summarizes a fragment, not a replayable request). |
| Update compactions replay the checkpoint message (cache plan phase A review fix) | Since the previous compaction, every regular request begins with the checkpoint message; the replay list is headed by the same projected message so the prefix matches from message 0, while `messagesToSummarize` keeps only new messages so summary semantics are unchanged |
| Replay requests inherit provider caching; standalone summary requests still opt out (cache plan phase A review fix) | `completeSummarization` takes `cacheOptOut` (default true); replaying callers pass false so breakpoints and routing match regular requests, split-turn/standalone callers keep the old opt-out |
| Prefix violations are surfaced with attribution, not raised (issue #41, cache plan phase B; deliberate DSH divergence) | DSH errors on transcript mutation because its extension model forbids it; pi's extension API intentionally allows `transformContext`, so the fork observes every provider-bound request at an AgentSession streamFn wrapper (one layer over the constructed streamFn, covering regular turns, retries, and the phase-A replaying compaction/branch-summary calls), attributes each change to the announcing subsystem or as `unexpected-*`, counts by cause in `SessionStats.prefixInvalidationsByCause`, and emits one `prefix_invalidated` diagnostic event only for unannounced changes. The compaction replay request is NOT append-only relative to the prior regular request (it swaps the trailing user turn for the summarizer instruction; the split-turn turn-prefix summary is a standalone context), so the compact flow announces "compaction" before the summarizer call as well as at the state reset. Announced expectations stay armed until the next stable request — not strictly one-shot — so multi-request flows (split-turn compaction issues two diverging summarizer requests) attribute every call, and an abandoned announcement cannot leak past the next normal turn |
| Per-request cache usage is attributed by request kind at the same streamFn wrapper, not derived from session entries (issue #42, cache plan phase C) | The wrapper already sees every provider request, so usage attribution has one observation point: a one-shot retry marker wins over an active summarizer-flow kind window (`compaction`/`branch-summary`), everything else is a `turn`. `retry` covers the three repeat paths that exist: the agent auto-retry continuation (`_prepareRetry`), the overflow compact-and-retry replay (`_runAutoCompaction` willRetry), and summarizer retries (`RetryCallbacks.onRetryAttemptStart` fires immediately before the re-issued call — a failed summarizer attempt itself stays under its flow's kind). The agent is idle while summarizer flows run, so a window cannot capture unrelated turns; kind attribution is deliberately separate from the phase-B expectation latch so the first regular turn after compaction is a `turn` (its rebuild miss is an invalidation, not summarizer usage). Aggregates are in-memory run-scoped (`SessionStats.cacheUsageByKind`) because the session log must not carry high-frequency per-request telemetry. The kind union carries only kinds a flow produces today; kinds are added when producers exist |
| Upstream's installer-managed update subsystem not ported (upstream 4af9d21d3) | It stages releases fetched from `pi.dev/api/installer/releases` and depends on the deleted `utils/version-check.ts`/`utils/pi-user-agent.ts`; both violate the zero-pi.dev-traffic policy (issue #32) and no fork install is ever a managed install (GitHub Releases channel only, `PI_MANAGED_INSTALL_ROOT` never set). On syncs, drop the subsystem (package-manager-cli.ts managed block, main.ts `cleanupManagedInstall` wiring, its tests, and the `docs/packages.md` managed sentence) the same way the telemetry files are re-deleted. |
| Auto-discovered extensions and skills sort by resolved path at collection (issue #49; DSH parity) | Tool and skills order sits at the front of the request prefix, so readdir-order enumeration (filesystem-dependent: ext4 htree re-bucketing, cross-machine restarts) reorders tools/skills after a restart and busts the provider cache for the whole conversation; lexicographic path order is machine-independent (DSH's deterministic default). Settings-declared order (packages array, settings entries), package manifests, builtin order, and the `toResolvedPaths` precedence ranks are untouched — collection output only feeds the within-rank order of the already-stable rank sort. One-time reorder on upgrade acknowledged: existing sessions may see a single first-request cache miss after upgrading. 2026-09-03 correction (#139/#140): the original flattened sort reordered a package's manifest-declared entries, breaking this row's manifest promise; collection now sorts path-keyed groups (a package is one group holding its manifest entry order), so only groups order — one further one-time reorder for mixed loose-file/package directories, pinned by a canonical-order test with a package declaring non-alphabetical entry order. |
| Replay summarizer calls carry the session routing id, `agent.sessionId` (issue #51; DSH parity) | The two requests built to replay the warm prefix (compaction and branch-summary summarizers) are exactly the ones that dropped the session's cache-routing key, so on providers with session-scoped routing (OpenAI `prompt_cache_key`, Mistral `x-affinity`) the replay landed outside the regular requests' cache bucket. Forwarding `agent.sessionId` — the value the agent loop spreads into every regular request's options — restores routing affinity; on DeepSeek implicit caching it is inert (DSH sends the exact session id on its compaction call). The id is forwarded, never minted, and the standalone split-turn turn-prefix request deliberately does not get it (it opts out of caching and keeps `completeSummarization`'s throwaway id). |
| blockImages toggles are announced as `settings-change` prefix invalidations (issue #53) | The rewrite is the feature, not a violation: `convertToLlmWithBlockImages` (sdk.ts) reads `settingsManager.getBlockImages()` on every request and replaces images in ALL messages — including already-sent ones — with placeholders (that is how image tokens leave the context), so a mid-session toggle is a deliberate full-prefix rewrite that previously surfaced as an unattributed `unexpected-history-change`. The AgentSession request observer now tracks the last-seen effective value and announces before diffing, so the bust counts under its true cause; the tracked value initializes from the first observed request, and semantics stay simple — value flip ⇒ announcement — while a flip that changes nothing on the wire (no images in history) records nothing because the stable request clears the expectation latch. |
| Prefix-monitor blind spot at the provider layer accepted and documented (issue #53; no code) — superseded by the wire-rewrite seam (issue #56) | The monitor observes requests at the AgentSession streamFn boundary; everything `packages/ai` mutates afterward is invisible to it. Two known rewrites: (a) Anthropic deferred-tools migration — on first-party models with `supportsToolReferences`, extension tools sit in `defer_loading` until first used, then migrate into the immediate list, rewriting the wire tools array and the `tool_reference` blocks in past tool results (one-time per tool, dynamic-tool sessions only, Anthropic's own token-saving feature); (b) auth-mode switch — OAuth adds the "You are Claude Code" system block and renames tools, a silent full bust. Candidate seam: a request-serialization hook in packages/ai. Deferred because that is an upstream-surface change in packages/ai for a one-time-per-trigger class of busts; revisit if dynamic-tool sessions on Anthropic become common. Superseded: the seam row below now reports both rewrites. |
| Key-gated e2e test verifies real cache behavior against a live provider (issue #55; DSH `request-cache.e2e.ts` pattern) | `packages/coding-agent/test/e2e-cache-verification.test.ts` is the only oracle for actual hit behavior (byte-equality and faux-provider tests cannot prove it): it drives a real 3-turn conversation with one tool turn plus a manual compaction against Anthropic, with the system prompt padded past the 1024-token cacheable minimum (run-marker-unique padding so request 1 is always a cold miss) and asserts cacheRead>0 on every response after the first, a compaction-kind cacheRead floor (>= 60% of the last regular turn's reported input+cacheRead, so a replay that diverges from the regular requests' message 0 cannot pass by writing a fresh block), the session routing id on the replaying summarizer request, and zero `unexpected-*` prefix invalidations. It self-skips without `ANTHROPIC_API_KEY`/`ANTHROPIC_OAUTH_TOKEN` (repo e2e convention), so CI stays hermetic; running it locally costs a few cents. |
| Provider-layer wire rewrites are reported through an optional packages/ai `onWireRewrite` seam and counted directly, not via the expectation latch (issue #56) | `StreamOptions.onWireRewrite?: (cause: string) => void` is the whole seam (additive, undefined-safe, no per-request allocation beyond the callback reference); `buildBaseOptions` forwards it so `streamSimple`-built requests carry it, and the Anthropic adapter fires `"provider-auth-mode"` when a request's auth mode differs from the observer's previous (first observation initializes silently, like `_lastBlockImages`) and `"provider-deferred-tool-load"` when a request anchors a deferred tool load no earlier request of that observer anchored (the `loadedToolNames` transition in `convertToolResult`, checked against the observer's anchored set). Reading the adapter corrected the issue's mechanism description: placement is marker-anchored, not first-use-migrating — a tool used after its marker stays deferred and its `tool_reference` rides the appended marker message, so the anchor request is the rewrite point. The AgentSession streamFn observer injects the callback into the options it forwards and counts each cause once per request, never on the first observed request; it deliberately does NOT call `expectInvalidation` — the fires land inside the wrapped stream call, after this request's context diff, and these transforms never appear in any later context serialization either (the adapter builds fresh wire structures and never mutates the context), so a latch would be cleared by the next stable request unconsumed and would mis-attribute the next unannounced divergence (the blockImages announce-then-diff pattern does not apply to wire-only rewrites). Adapter transition state (`lastOAuth`, the anchored deferred-tool set) lives in a `WeakMap` keyed on the `onWireRewrite` callback: per-session attribution (the coding-agent monitor injects one stable bound method), tracking dies with the observer, and `StreamOptions.onWireRewrite` documents the stable-callback contract. Adjacent stats gap kept documented: requests whose stream ends via `end()` without a result (the `hasResult` fallback in `lazyStream`'s `forwardStream`) stay uncounted by the cache-usage tracker — every built-in adapter resolves `result()` (the terminal done/error event resolves it), so the gap is reachable only by custom API streams lacking `result()`, and counting them would need an EventStream ended-without-result signal: upstream API surface for a diagnostic. |
| Fork feature placement follows pi's minimal-core rule: extension-first where the public extension API allows, core only with a recorded reason (issues #69, #90) | The extension API exposes the seams the fork's core features use (`tool_call` block-with-reason, `tool_result` content replacement, `getActiveTools`/`getAllTools`/`setActiveTools`, `ctx.ui.confirm`). 2026-08-28 re-evaluation: the permission engine's original blockers were overstated — config can live in policy files (`.pi/permissions.json` over `~/.pi/agent/permissions.json`) rather than settings, and permissions were always opt-in so no builtin slot is needed — so the full engine now ships as `examples/extensions/permission-policies.ts` (zero core surface beyond re-exporting the evaluator; interactive `ask` approval via dialog, which core mode cannot offer; 2026-08-31: the engine moved from the copy-me example to a config-gated builtin, see the next two rows). 2026-08-29 (issue #70): the core `tools.permissions` mode is removed — AgentSession enforcement and visibility filtering, and the SettingsManager surface, are gone; the evaluator, capability vocabulary, and their exports stay as library API for the extension. The output cap stays core as a deliberate default-on product choice (the README advertises the 200 KB cap out of the box); the extension API could host the mechanics (`tool_result` replacement plus session-dir access), but an extension would make the guarantee conditional on extension load and reload semantics. 2026-08-31 audit correction: extensions DO see provider requests (`before_provider_request` serialized payload, `before_provider_headers`, `after_provider_response`) and CAN replace compaction wholesale (`session_before_compact` `{cancel, compaction}`, demonstrated by `examples/extensions/custom-compaction.ts`), so the earlier "provider-request-path positions extensions cannot reach" wording was overstated. What extensions genuinely cannot reach: the streamFn position (per-request prefix diffing, retry-kind attribution, summarizer request-kind windows, `onWireRewrite` injection), the agent-loop tool deadline (`timeoutMs`), and the startup resume path (extensions load after recovery runs; `ReadonlySessionManager` exposes no append). Session repair, deterministic ordering, and the request monitor are core on that position; the compaction replay is core on byte-exactness, not on a missing hook — an extension rebuilding the summarizer must reproduce the projected prefix byte-for-byte across system prompt, tool list, and message conversion, and any drift busts the cache the feature exists to protect — and the replay also fixed the SDK default (`SummarizationPrefix` required), which is core API either way. |
| Placement ladder for new fork features (2026-08-31 audit) | Existing config first, then a gated `builtInExtensions` factory, then a copyable example extension, core with a ledger row last. Pi ships a bundled-extension channel the fork has not used: `builtInExtensions` (packages/coding-agent/src/extensions/index.ts, the hidden llama.cpp entry; `main.ts` merges caller-provided `extensionFactories`) runs through the public extension API; its only upstream-owned touch is the `builtInExtensions` array literal and its imports (see the changed-files row), so upstream syncs conflict at most there. An opt-in builtin no-ops unless its config exists (a permission-policies factory activates only when a policy file is present; shipped 2026-08-31, see the next row), giving integrated-but-opt-in with no copy step and no core wiring. Core stays reserved for the unreachable positions the placement row above lists. |
| Permission policies ship as a hidden config-gated builtin (2026-08-31; first rung-2 application of the ladder) | `builtInExtensions` gains a `permission-policies` entry backed by the fork-owned `src/extensions/permission-policies/index.ts (folder with README)`, activating only when a policy file exists: `~/.pi/agent/permissions.json` always, `.pi/permissions.json` only in a trusted project — a builtin loads in every directory, so an untrusted checkout's policy file must not steer enforcement (under the copy-me model the user's install was the opt-in); trust granted mid-session starts call-time enforcement at the next tool call and visibility changes at the next session_start or /reload. With no policy file the extension is fully inert (no visibility change, no call-time decisions), so default sessions are untouched and core still performs no enforcement. The copy-me example is deleted: a copy plus the builtin would run the engine twice (double `ask` dialogs); `examples/extensions/read-only-mode.ts` stays the authoring example. `hidden: true` keeps the inert entry out of the startup Extensions list; an active one is observable through its effects (deny reasons name the policy file, `ask` opens the dialog) |
| Model handoff ships as a hidden config-gated builtin (2026-09-01; issues #106 through #111) | `builtInExtensions` gains a `model-handoff` entry backed by fork-owned `src/extensions/model-handoff/index.ts (folder with README)`, activating only when `handoff.json` names at least two registry-resolvable tiers: `~/.pi/agent/handoff.json` always, `.pi/handoff.json` only in a trusted project, project entries winning name collisions. The active model hands the whole conversation to a tier by calling `switch_model`; the agent loop already re-reads the session model before every provider request, so the next turn of the same run comes from the target and the tool result is the baton — zero core surface beyond the array entry, everything through `registerTool`, `pi.setModel`, and factory-scope event subscriptions (`session_start`/`agent_start`/`agent_settled`/`model_select`; never module scope, which would leak across session switches). Refusals (already-active tier, any earlier baton holder in the settled run, missing auth, tier vanished from the registry) change nothing; `returnAfterRun` returns control at `agent_settled` and a manual switch cancels it; deactivation on a shrunken merged set subtracts the tool from the active list, with reactivation scoped to the extension's own deactivation so an earlier-loaded extension's removal (a permission-policies hide rule) survives. Known edges, recorded on PRs #116/#117: a prompt submitted in the instant between idle and the settle handlers, or another extension's `agent_settled`-triggered turn, can be served by the delegatee rather than the requester; a mid-session tier-set addition regenerates the enum (announced as `tool-set-change` since issue #120, see the next row). |
| Tool-schema changes announce as `tool-set-change` (2026-09-01; issue #120) | `setActiveToolsByName` fingerprints the active tools with the monitor's own `serializeTools` (name plus stringified parameters) and arms `tool-set-change` when the fingerprint changes, replacing the name-list-only check: a schema-only re-registration — model-handoff regenerating its `switch_model` target enum on a config reload — rewrites the request's tools section with unchanged names and previously surfaced as `unexpected-tools-change` with a diagnostic event. Reusing the monitor's serialization keeps the fingerprint exactly as narrow as the prefix diff, so description-only changes stay unannounced exactly as before (the monitor never compares them). Timing note (mechanism pinned by #127): `message_end` fires for user messages too, so a re-registration driven from it armed before any request carried the change; the session's first request is trivially stable and cleared the arm per the one-latch design, leaving the change to surface on the next request as unexpected. No monitor race — re-registrations belong at refresh points between runs (`session_start` refires, reloads, `agent_settled`), which is what the attribution test drives. The announce site is the existing one in `agent-session.ts`, where the name-list check it replaced lived. |
| Runs wait out an in-flight settle emission (2026-09-01; issue #122) | `_runAgentPrompt` awaits the in-flight `agent_settled` emission before starting (latch armed before the emission begins, so even a message triggered from the first handler's synchronous body parks), because the session reports idle during settle handlers by pinned contract (regression #6363) and a run started in that window raced settle handlers — visibly, model-handoff's `returnAfterRun` switching the model back while the user's prompt was already served by the delegatee. Core, not extension, because extensions cannot gate `prompt()`. Accepted edges on PR #123: an abort during the window cannot cancel the parked prompt (it targets the finished run), a never-resolving settle handler parks later prompts (pre-existing hang class, new symptom), and command dispatch / input hooks still observe mid-settle state (pre-existing, harmless). |
| Upstream is fetch-only: the fork never files issues, PRs, or comments on `earendil-works/pi` (2026-09-03; maintainer decision, ending the #130 round) | The 2026-09-02 placement audit proposed contributing four additive core diffs upstream (issue #130 and children); four issues were filed (earendil-works/pi#9025 through #9028) and withdrawn the next day when the maintainer set the standing policy: fork features exist only in this fork, and the upstream relationship is one-way sync. The withdrawn shells remain upstream (GitHub offers no issue deletion) as closed/not-planned with their content replaced by a withdrawal note; no PR was ever opened from the round. The round's one salvage, the pre-flight review findings, landed in the fork as #139/#140. The `upstream` git remote is fetch-only and must never be pushed to (this clone's push URL is intentionally set to `DISABLED` — `git remote set-url --push upstream DISABLED` — so accidental pushes fail; do not "fix" it back). Recorded in AGENTS.md alongside this row; reopening any upstream contribution requires the maintainer to say so explicitly. |
| Placement re-audit against the extension API's actual wiring (2026-09-04; issues #144–#146): every current placement verified correct, core stays reserved to six positions | The audit checked each core feature against what the extension API really reaches, not what its docs imply. The six core positions: (1) the `streamFn` observer — `before_provider_request` is agent-loop-level (`onPayload`) and never fires for summarizer traffic (compaction/branch-summary calls pass their own options straight to the streamFn; `before_provider_headers` does fire there, its wrapper is unconditional in sdk.ts), and no hook carries request-kind identity; (2) the agent-loop tool deadline — `timeoutMs` is public on `ToolDefinition`, so extensions already set deadlines on tools they register, but nothing but core can impose one on built-in or third-party tools; (3) the startup resume path — the ExtensionRunner is constructed after recovery runs and messages are adopted, `ctx.sessionManager` (`ReadonlySessionManager`) exposes no append, and the `newSession`/`fork`/`switchSession` `setup` callbacks hand extensions a writable SessionManager only for replacement sessions, never the one being resumed (the torn-tail recovery read is kept library surface with no startup wiring by design: upstream's tolerant loader already drops the torn line, the validator reports it); (4) loader-internal resource ordering (extensions load after enumeration); (5) TUI chrome (assistant-message internals, generic tool rows, footer stats — the extension UI surface reaches custom entry types, markdown transforms, dialogs, and footer status text only); (6) the `/session` panel (hardcoded command over `getSessionStats`, no hook). Verified plugin-shaped and correctly placed: permission-policies and model-handoff are pure public API whose only upstream-owned touch is the `builtInExtensions` array literal. Standing rule: a new feature is an extension unless it needs one of the six positions; when a feature is blocked only by a missing export (e.g. an unexported rewrite helper), prefer adding the export over moving code into core — one additive line beats a permanent sync liability. |
| Standalone tarball vendors chord with its dependencies stripped from the vendored manifest (2026-09-04; fork.8 standalone regression) | npm counts a bundled package's dependency closure as covered by the bundle and then skips extracting the root-level copies: v0.85.0-fork.8 installed with an empty `node_modules/esbuild` while npm exited 0, and `pi` crashed at startup inside chord's `import { build } from "esbuild"` (reproduced minimally: root dep `esbuild@X` + bundled package requiring `esbuild@X` → empty dir, no extraction, exit 0; npm 11.13.0, flaky across runs). Fix: `stageStandaloneDirectory` deletes `dependencies` from the vendored chord's staged manifest (they are hoisted to the root manifest as `vendoredDependencies`, and Node resolves them from the root node_modules for chord's code), refusing to stage any vendored dependency not present in the root manifest. Verified end to end by repacking the fork.8 tarball with only that field stripped: esbuild and the `@esbuild/linux-x64` optional extract, `pi --version` runs. Platform safety: esbuild stays a registry-resolved root dep, so its `@esbuild/*` optionals pick the right platform per install machine. `install.sh` additionally runs the installed binary as a startup smoke test and retries once from a clean package directory on failure, so any future npm extraction surprise surfaces at install time with a self-heal instead of a first-prompt crash |
| Opt-in phone-home (2026-09-05; partial reversal of issue #32's "deleted rather than flagged off") | Two behaviors can meaningfully return and ship as settings, default off, asked once by the first-run wizard: `updateCheck` fetches `api.github.com/repos/R-Dson/pi/releases/latest` at interactive startup and warns when a newer fork release exists (semver `gt` against the running VERSION, silent on any failure, PI_OFFLINE respected, a 10 s abort timeout bounds hung connections; fork-owned `core/update-check.ts`), and `providerAttribution` re-adds the optional OpenRouter attribution headers with fork values, read per request in sdk.ts's transformHeaders so mid-session toggles apply. Install/version pings stay dead permanently (no fork backend exists to receive them); the remote model catalog stays manual (`pi update --models`); automatic extension-update checks stay dead (`pi update --extensions` is explicit). Core, not a builtin extension: the wizard and /settings are core TUI in already-fork-diffed files, the `before_provider_headers` hook carries no provider identity so attribution cannot live in an extension, and the startup position is the one the purged check occupied. The wizard gate (same-day amendment after the gate proved invisible on existing installs): the fork drops upstream's `PI_EXPERIMENTAL` requirement entirely and runs the wizard once per install — fresh installs, plus existing installs whose settings lack both privacy keys (skipping records the decline so it never re-asks; the theme question is always first, preselected to Automatic so confirming without navigating adopts it (superseded for installs with a set theme 2026-09-06: the theme step is skipped entirely — see the wizard theme-skip row)). Default sessions remain fetch-free: the no-tracking test drives the armed startup path through a real default-off SettingsManager, `update-check.test.ts`/`sdk-openrouter-attribution.test.ts`/`first-time-setup-fork.test.ts` cover check logic, header gating, and the fork gate |
| Placement re-audit, full fork diff vs upstream/main at da840b621 (2026-09-06; claims checked at code sites rather than docs): every placement re-confirmed, no feature moves, the 2026-08-31 ladder stands unchanged | The six core positions, re-verified with their evidence: (1) the streamFn observer — `onPayload` is an Agent-level option (sdk.ts hands it to the agent; the sdk streamFn closure wires only `transformHeaders` unconditionally) and `completeSummarization` calls the streamFn with its own options, so `before_provider_request` never fires for compaction/branch-summary traffic and no provider event carries request-kind identity; (2) the tool deadline — `timeoutMs` enforcement is loop-local in `executePreparedToolCall`, extensions set it only on tools they register, and their one lever on a built-in is a same-name `registerTool` override (wholesale registry replacement, not in-place mutation, and silent: the tool-conflict diagnostic fires only extension-vs-extension, and the builtin-conflict warnings in interactive mode cover commands and shortcuts, not tools), so per-tool deadlines on builtins as shipped remain core-only; (3) resume repair — `appendInterruptedTurnResults` runs in `createAgentSession` before the ExtensionRunner is constructed, and `ctx.sessionManager` is `ReadonlySessionManager` (getters only, no append), so an extension cannot repair a resumed session's dangling calls; (4) collection ordering — the `comparePaths` sort runs in `resolve()` before extension modules load (the pre-trust bootstrap loads a few modules earlier, but the comparator takes no extension input); (5) TUI chrome — strictly unreachable are the hidden-thinking preview internals and the built-in footer's stats rendering; `setFooter`/`setHeader`/`setEditorComponent` and same-name tool override can replace whole regions, which is replacement, not extension of the built-in rendering; (6) `/session` — hardcoded command over `getSessionStats`, no hook. Output bounds stay core as the recorded default-on product choice: the `tools.maxToolOutputBytes` setting requires core because no settings reader exists anywhere in the extension surface (both builtins read JSON config files for exactly that reason). Named contingency for the largest TUI sync liability: assistant-message.ts carries 317 changed lines (298 insertions) for the thinking preview plus fade, and the 2026-09-03 sync already cost one forced re-port (upstream's click-toggle dropped for it); on the next forced re-port of this file, cut the fade layer first (the OSC 11 background query in `init()`, `Theme.fgValue`, the per-word `fadeTailLines` coloring) — the header-plus-tail preview carries most of the value at a fraction of the entanglement |
| Session-writer flush barriers and the persistenceMs metric descoped (plan 2.5 and the persistence fields of plan 6; recorded 2026-09-06 by the code-review spec pass) | Upstream's SessionManager appends every entry with a synchronous `appendFileSync` of one JSON line (session-manager.ts write paths); there is no buffered writer, so there is nothing for `flush()`/`close()` barriers to drain, no queue to order, and no persistence window to measure. The crash class the barriers guarded against is covered by per-entry appends plus the tolerant loader, the validator, and resume repair (torn tail skipped, dangling final-turn calls repaired at startup). The rest of plan 6 shipped or is separately descoped: `toolOutputBytes`/`truncatedToolOutputBytes` live in `SessionStats`, cache metrics in `cacheUsageByKind`, and the projection-side metrics fell with the projection-cache skip decision above |
| Canonical tool-execution pipeline descoped as an umbrella (plan 3.1; recorded 2026-09-06 by the code-review spec pass) | The single ordered executor (resolve, validate, visibility, permission, persist) was never built; upstream's agent loop remains the executor and each pipeline concern shipped at its own seam with its own row: tool deadlines in the loop (`timeoutMs`), output bounding at the `afterToolCall` hook, capability/visibility filtering and permission evaluation in the opt-in permission-policies builtin. Consequence for plan 4.2: "filtering tool visibility happens before prompt construction" holds only in sessions using that builtin (hide rules subtract tools from the active set before the next prompt rebuild); a default session exposes every active tool, matching upstream. Plan 3.2's execution scopes were already skipped by their own decision row; what would reopen this is a need for executor-wide policy, e.g. cross-cutting auditing of every stage |
| Wizard theme step skipped when settings already carry a theme (2026-09-06) | The wizard gate re-runs for every install missing the privacy keys (i.e. all pre-privacy upgrades), and asking the theme question there was worse than noise: the list preselects Automatic, so confirming overwrote an explicitly set theme (only cancel preserved it). `getThemeSetting() !== undefined` now starts the dialog at the privacy questions, the submitted result omits `theme` so `showFirstTimeSetup` never writes one, and the wizard's unconditional recolor to the detected appearance is guarded — a set theme keeps coloring the dialog. Fresh installs (no theme key) see the theme step unchanged |
| Extension API `ctx.ui.transientStatus` (2026-09-06; footer-overlap round two) | Exposes the footer's existing transient slot (the ctrl+t mechanism, `FooterComponent.setTransientStatus`) to extensions: turn statistics and similar read-once feedback flash on the pwd line instead of occupying a chat status row per turn — `showStatus` appends a transcript row that persists once anything follows it. Interactive mode wires the method to the footer; the runner's no-op UI context (print/json, no footer) ignores it. Placement follows the setStatus division: TUI chrome stays core (position 5), the extension decides when. The dogfooding tps extension switched to it (see the tps row) |
| Thinking blocks hidden by default (2026-09-06; `getHideThinkingBlock` default flipped `false` → `true`) | Fork product choice: the hidden mode is the fork's live-preview treatment (header with timer plus tail excerpt, ctrl+t to expand), so fresh installs start compact; upstream's `false` default renders full blocks inline. Only the getter default changes: settings carrying an explicit value (any install that ever pressed ctrl+t) are unaffected and the toggle still persists the key, while installs that never set it flip to hidden on upgrade — recorded in the changelog. Companion doc edit: settings.md's `hideThinkingBlock` row states the fork default |
| Releases are dispatch-only; `release.mjs` never runs here (2026-09-06 codification) | `release:patch` ran once in the fork on 2026-09-06 before the dispatch flow was written down, bumping repo versions to 0.85.5 and tagging a plain `v0.85.5` — nothing consumes either (published fork releases are tagged by the workflow at `<version>-fork.<run>`). The stray bare tag was deleted (local + origin) on 2026-09-08 when the post-sync review re-flagged it, ahead of upstream's own 0.85.5 release — the same remedy as the 2026-09-04 `v0.85.0` deletion; the bare `v0.85.0`/`v0.85.1` tags remaining in this clone are upstream's fetched tags, local only, and stay. One-time cost: when upstream releases their 0.85.5, that sync must fold the fork's `## [0.85.5]` changelog sections into upstream's; versions already match, so only the duplicate heading collides. AGENTS.md's Releasing section now documents the dispatch flow |
| Display name is "Pi Fork" on fork-owned surfaces only (2026-09-06) | README title, the terminal title (`APP_TITLE` fallback in config.ts), GitHub release titles, the update notice, and the repo description carry the pun; functional identity stays untouched: repo URL, `@r-dson/*` package names, the `pi` binary, the `.pi` config dir, and `APP_NAME` (which derives the `PI_*` env vars) all keep their names, since renaming any of them breaks installs, user data, or upstream merges for zero function |
| Sub-agents ship as the built-in `task` tool in core as a deliberate divergence from upstream's design principles, not an extension-impossibility (2026-09-12; rationale rewritten 2026-09-13 by the placement audit) | Upstream's stated philosophy excludes sub-agents from core ("It intentionally does not include built-in MCP, sub-agents, permission popups, ...", docs/usage.md Design Principles) and demonstrates the intended shape as `examples/extensions/subagent`: subprocess `pi --mode json -p --no-session` invocations with live JSON-line updates, custom renderers, and parallel/chain modes — an extension path that needs none of the positions this row originally cited. The recorded blockers are consequences of the fork's in-process design choice, not laws: `maxSubAgents` could live in a JSON config file the way both config-gated builtins read theirs (no settings reader exists in the extension surface — the same evidence that keeps `tools.maxToolOutputBytes` core), and the session's unwrapped `agent.streamFunction` plus the shared `beforeToolCall`/`afterToolCall` hooks matter only when the sub-agent runs inside the parent process. In-process won on: sub-agent tool calls pass live through the same permission gate, image normalization, and output bounding (a subprocess only approximates this by reloading extensions), no spawn cost per task, `Task N` preview windows share `preview-window.ts`, usage rides the tool result, and `unwrapStreamFn` keeps sub-agent requests off the prefix observer (interleaved contexts would register as unannounced invalidations and corrupt the cache diagnostics). One-layer depth is structural: sub-agents inherit the active toolset minus `task` itself, so there is nothing to recurse through. Default-active is the product choice (`defaultTools` opts out; `createCodingTools`/`createReadOnlyTools` exclude it); the divergence from upstream's excluded list is accepted and recorded, not papered over as unavoidable |
| Skills-anywhere expansion ships in core at upstream's existing expansion sites; the pi-tui autocomplete change is additive and opt-in (shipped 2026-09-06 in 0.85.5; row recorded 2026-09-13 by the placement audit — the feature had shipped without a ledger row) | The expansion mechanics were extension-reachable (the `input` transform event rewrites prompt text before submission, upstream's demonstrated pattern in `examples/extensions/input-transform.ts`), but the rest is not: the multi-block rendering (interactive-mode's user-message loop and the HTML export are TUI chrome, position 5), the mid-text `/` autocomplete (pi-tui editor), and coverage of every input path — the agent-session sites where upstream's leading-token `_expandSkillCommand` already lived (the prompt path gated on `expandPromptTemplates`, and steer/followUp), so RPC and SDK sessions expand identically instead of only the interactive prompts an extension hook sees. The pi-tui delta is `SlashCommand.midTextInvocable` (commands opt in; consumers that do not set it are unchanged) plus an editor trigger for `/` at whitespace boundaries — an additive library capability with the coding-agent's skill commands as the shipping consumer |
| Placement-hardening round from the 2026-09-19 fork review (2026-09-20): thenRun bash-facet gate, task deadline, replay extraction, docs truth | Four fixes. (1) The fused thenRun command now fires a real `tool_call`/`tool_result` event pair for its bash facet (`toolCallId` `<parent>:thenRun`) through the session's fused-command executor (`createSessionFusedCommandExecutor` in then-run.ts, wired in `_buildRuntime` beside the bash options; lazily reads the extension runner and the session-built base bash definition (not the registry's entry — its tools are hook-wrapped while the executor fires its own gate events, and its active-tool filtering would make a deactivated bash silently drop the session's bash settings to a fresh default definition), so reloads and rebuilds apply and the session's `commandPrefix`/`shellPath`/operations are inherited), deleting the permission-policies special case that peeked at `thenRun.command` — any gating extension now sees fused commands, ask dialogs included; a blocked command appends `[thenRun:skipped]` with the reason and the edit result stays non-error (the mutation is kept); plain factories without a session keep the fresh local ungated bash — the same fallback covers sessions built with `baseToolsOverride` (the embedder supplied their own edit/write AgentTools; the session cannot inject into constructed tools, and `createSessionFusedCommandExecutor` is exported for embedders who want the gate). A throwing gate handler never rejects out of the fused path (the mutation already succeeded; a raw rejection would drop its output): gate errors map to the failed outcome with an extension-error message; the tool_result observation never throws (the runner catches per handler), and a blocked fused command emits only the tool_call, no tool_result, matching the agent loop's blocked-call path (2026-09-20 review fixes). (2) The task tool gains the `taskTimeoutMs` setting (top-level beside `maxSubAgents`, default 600000, 0 disables, read per spawn so settings changes apply without a rebuild; invalid values (non-finite, negative, above the 2^31-1 setTimeout ceiling) are rejected at both the setting getter and the tool's deadline setup, mirroring the agent loop's timeoutMs range check — setTimeout silently coerces out-of-range values to an immediate or 1ms deadline (2026-09-20 review fix: the tool previously checked only the ceiling, so a NaN or negative embedder getter silently disabled the deadline)) and per-line truncation (2000 chars) in its transcript excerpts. (3) Replay construction moves to fork-owned `core/compaction/replay.ts` (`SummarizationPrefix`, the persona/format instruction builders, the shared `buildReplaySummarizationContext` — previously duplicated inline in branch-summarization.ts — and `buildReplayMessages`), and branch replay units to `core/compaction/branch-units.ts`; compaction.ts/branch-summarization.ts keep upstream's function structure and delegate, `SummarizationPrefix` stays re-exported from compaction.ts, and the two checkpoint-format prompt constants moved with the builders (their bytes are fork-pinned). `test/compaction-replay-golden.test.ts` pins the compaction fresh/update/turn-prefix and branch-summary request contexts and options by SHA-256 digest under a fixed clock (sessionId stripped from the options digest — the standalone path mints a throwaway id), so the move is provably byte-identical and any future drift lands in that test deliberately (fixtures carry tool-call/result pairs and a dangling trailing call, pinning the branch pairing/repair bytes, and the fresh path passes customInstructions for the instruction builder's focus branch; executor unit tests with a stub runner cover block/mutation/result-override/throwing-handler paths, and policy-file session tests cover deny and ask end to end). (4) `docs/usage.md`'s Design Principles now state the fork's one divergence (the built-in task tool) instead of carrying upstream's "no sub-agents" sentence verbatim. Spec: `docs/fork/placement-hardening-spec.md` |
| Patch-surface policy for upstream-owned files (2026-09-20, codified after measuring the replay extraction): three kinds of changes only, and a two-part bar for moving upstream code into fork files | Allowed on upstream-owned files: (1) deletions of features the fork rejects (telemetry, the installer subsystem), (2) minimal additive seams (an options field, a settings getter, one wiring line, an optional type field), (3) defect fixes inside hunks already forked. New logic and new features live in fork-owned modules from day one; readability polish of upstream files is never a reason to touch them. Measured basis (2026-09-20, merge-base diff before/after the replay extraction): the move shifted ~290 lines into fork files and cut fork-authored insertions in the compaction pair from 271 to 111, but hunk count moved only 29 to 27 — moving code is conflict-neutral, because deletion hunks collide with upstream edits exactly as in-place edits do. A move is therefore justified only by one of two things, never aesthetics: it kills real duplication (the two inline copies of the replay-context builder the extraction merged), or it enables a test gate on behavior whose failure is silent (the golden digest test — a drifted replay still compacts fine, it just stops hitting the provider cache). The port obligation a move creates (upstream edits to moved code resolve by porting into the fork file, digests verifying the port) is load-bearing and recorded per file in the table below |

## Changed Upstream Files

Every upstream source file and shared test utility the fork modifies, and the assumptions each change rests on. Plain per-feature test files are covered by their feature's row instead.

| File | Why | Fork module called | Assumptions | Tests |
| --- | --- | --- | --- | --- |
| `packages/coding-agent/src/core/tools/edit.ts`, `write.ts`, `bash.ts`, `output-accumulator.ts` | `thenRun` fused-verification parameter on edit/write (command runs inside the mutation-queue slot; mutation failure skips the command, command failure keeps the mutation and reports both outputs), and truncated shell full-output spill redirected to `<sessionDir>/artifacts/<sessionId>/` for persisted sessions (temp dir fallback otherwise). Design from NVlabs/SoL-Pi (Action Fusion, ObservationPack archive), implemented natively | `core/tools/then-run.ts` | Schemas add one optional field each, so existing calls and the strict-prefer constrained sampling are unchanged; the fused command executes through the session's bash definition (its options — `commandPrefix`, `shellPath`, operations — are inherited via the fused-command executor `createSessionFusedCommandExecutor`, wired in `_buildRuntime`; plain factories without a session keep the fresh local bash, ungated) and fires a real `tool_call`/`tool_result` event pair for its bash facet (`toolCallId` `<parent>:thenRun`), so every gating extension judges it like any bash call — the permission-policies special case is deleted (2026-09-20 seam fix; a blocked command appends a `[thenRun:skipped]` marker with the reason and the mutation result stays non-error; core output bounding is deliberately not applied to the inner command because the outer edit/write bound covers the combined content); `OutputAccumulator` takes an optional `dir` and only creates the spill file when output exceeds the truncation limits, so normal commands write nothing; spill files accumulate like the existing output-bounds artifacts (retention undecided, uniform with that path); the spill-dir persistence proxy is `getSessionFile()` truthiness, which can lag `isPersisted()` in the window before a session file is assigned — the fallback direction is safe (temp dir) | `packages/coding-agent/test/then-run.test.ts`, `packages/coding-agent/test/suite/then-run-gate.test.ts` |
| `packages/ai/src/models.ts` | `cacheWriteReadRatio(model)` helper: cache-write ÷ cache-read cost rate (0 for free writes, undefined for free/unrated reads) for pricing context rewrites (design: NVlabs/SoL-Pi compaction economics) | none (pure function beside `calculateCost`) | Base rates only — optional `cost.tiers` are ignored, matching the single-decision-ratio use; no behavior change to existing exports | `packages/ai/test/cache-write-read-ratio.test.ts` |
| `.github/workflows/ci.yml` | Fork runs stacked PRs whose bases are feature branches, so CI must not filter `pull_request` to `main`; `workflow_dispatch` allows manual runs | none (trigger-only change) | Trigger-only change; job steps (build, check, test) identical to upstream, so upstream merges only ever conflict on the `on:` block | The CI run on every stacked PR |
| `packages/coding-agent/src/core/session-manager.ts` | Extraction of pure projection into `core/sessions/projector.ts` | `core/sessions/projector.ts` | Re-export surface keeps external imports stable: all previously-exported entry types, `CURRENT_SESSION_VERSION`, and projection functions are re-exported from session-manager.ts, so no other file changes. `loadEntriesFromFile` and `SessionManager.open` stay tolerant and silent; validation/recovery live in the validator module. `buildSessionPath` terminates on cyclic ancestry: a revisit ends the chain like a broken parent reference (the same degradation the projector already applies to missing parents; the validator reports the cycle as `cyclic-ancestry`). `SessionManager.getBranch` (issue #80) carries the same visited-set guard with a comment pointing at the projector, so the manager-level walk cannot hang on hand-edited cyclic files either. 2026-09-21 sync: upstream's canonical-boundaries work landed its projection types in session-manager — the port moved them into the projector with the rest: `UsageEntry` (kind'd usage entries), `ContextEditEntry`/`ContextEditableContent` (append-only context edits), `ProjectedSessionEntry`/`SessionProjection`, and the standalone `buildSessionProjection` (context-edit application, newest-compaction-only checkpoints); session-manager keeps upstream's function structure and re-exports them | `packages/coding-agent/test/session-fixtures-golden.test.ts`, `packages/coding-agent/test/session-projector.test.ts`, `packages/coding-agent/test/session-manager/tree-traversal.test.ts` (getBranch cyclic-ancestry termination), `packages/coding-agent/test/session-context-edit.test.ts`, `packages/coding-agent/test/suite/agent-session-boundaries.test.ts` |
| `packages/coding-agent/src/cli/args.ts` | `--validate-session <path>` flag and help entry (one-shot validate command) | `core/sessions/validator.ts` | Diagnostic-only flag parsed like other value flags (missing value is an args diagnostic error); no other flag or runtime behavior changes | `packages/coding-agent/test/args.test.ts` |
| `packages/coding-agent/src/core/bug-report.ts`, `packages/coding-agent/src/modes/interactive/bug-report.ts`, `packages/coding-agent/src/core/bug-report-upload.ts` | Upstream's bug-report feature (arrived with the 2026-09-21 sync), fork-adapted to the zero-telemetry policy: the exported environment block carries no `userAgent` (the `pi-user-agent.ts` util is deleted fork-side) and `redactSettings` strips nothing — the fork's `Settings` never had `trackingId` | none (deletion of upstream references only) | The report is user-initiated via `/bug`: a consent dialog offers "Upload Report" (POST of the redacted bundle to the Radius gateway's `/v1/bug-reports`, anonymous unless the user attributes their Radius account — see the traffic-audit row) or a fully local "Export as Zip"; no field that identifies installs beyond version/platform, matching the runtime traffic policy row | `packages/coding-agent/test/bug-report.test.ts`, `packages/coding-agent/test/interactive-mode-bug-report-hint.test.ts` |
| `packages/coding-agent/src/main.ts` | Early one-shot branch (like `--export`) that prints the validation report and exits | `core/sessions/validator.ts` | Runs before session creation and stdout takeover; exit 0 when clean or warnings-only, exit 1 on error-severity issues or unreadable file; `validateSessionFile` runs entries through the same migration loading applies (v1/v2 -> v3) before structural checks, so valid legacy sessions do not false-positive while genuinely malformed files still fail | `packages/coding-agent/test/session-validator.test.ts` |
| `packages/agent/src/types.ts` | Additive optional `timeoutMs` field on `AgentTool` | none (type-only) | Optional field: all existing tool definitions compile unchanged and behave as before; no default timeouts | `packages/agent/test/agent-loop.test.ts` |
| `packages/agent/src/agent-loop.ts` | Timeout enforcement in `executePreparedToolCall`: tool signal aborts on parent signal or deadline; deadline expiry yields the standard error tool result | none (loop-local) | Uses stdlib `AbortSignal.any` plus, since #139/#140 (2026-09-03), an `AbortController` with an unref'd cleared-on-settle timer — `AbortSignal.timeout` could not be cancelled, leaving bounded per-call residue for up to `timeoutMs` after early settlement; an explicit range check preserves the invalid-value terminal-error contract `setTimeout` would silently coerce to 1ms; the never-settling-tool case is pinned by a test; tools without `timeoutMs` receive the run signal unchanged | `packages/agent/test/agent-loop.test.ts` |
| `packages/coding-agent/src/core/agent-session.ts` | Output bounding appended to the existing `afterToolCall` hook, after extension interception and image normalization; the hook also accumulates truncated-byte/artifact counters and `getSessionStats` derives tool output bytes plus the new `SessionStats` fields | `core/tools/output-bounds.ts` | Hook returns an override only when a bound triggered or an extension/image hook already did; under-threshold results keep the exact executed result object. Artifact spill only when `sessionManager.isPersisted()`, under `<sessionDir>/artifacts/<sessionId>/`. Truncated/artifact counters are in-memory (process-run scoped, reset on resume); `toolOutputBytes` is entry-derived and session-wide | `packages/coding-agent/test/suite/tool-output-bounds.test.ts`, `packages/coding-agent/test/output-bounds.test.ts`, `packages/coding-agent/test/agent-session-stats.test.ts` |
| `packages/coding-agent/src/core/settings-manager.ts` | New `tools.maxToolOutputBytes` setting: interface field + `getMaxToolOutputBytes()`; default imported from the fork module | `core/tools/output-bounds.ts` | Additive optional nested setting; existing deep-merge/in-memory semantics unchanged; values <= 0 disable bounding (interpreted in the fork module, not the getter) | `packages/coding-agent/test/settings-manager.test.ts` |
| `packages/coding-agent/test/suite/harness.ts` | Optional `sessionManager` harness option for persisted-session tests | none (test utility) | Defaults to `SessionManager.inMemory()`; all existing harness callers unchanged | `packages/coding-agent/test/suite/tool-output-bounds.test.ts` |
| `packages/coding-agent/src/core/extensions/types.ts` | Additive optional `capability?: ToolCapability` on `ToolDefinition` (type imported from the fork module); issue #80 adds additive optional `timeoutMs?: number` on `ToolDefinition` with `core/tools/tool-definition-wrapper.ts` forwarding it (both wrapper directions) onto the runtime `AgentTool`, whose agent-loop enforcement the packages/agent row already covers — without the forwarding the field documented in docs/extensions.md would be a silent no-op, since every coding-agent tool reaches the loop through `wrapToolDefinition` | `core/tools/permissions.ts` | Optional fields: existing tool definitions and extension code compile unchanged and behave as before (no deadline set); `packages/agent`'s `AgentTool` is untouched, so capabilities live only in coding-agent definitions while `timeoutMs` rides the existing field pi-agent-core already enforces | `packages/coding-agent/test/permissions.test.ts`, `packages/coding-agent/test/suite/tool-permissions.test.ts`, `packages/coding-agent/test/agent-session-dynamic-tools.test.ts` (timeoutMs forwarding) |
| `packages/coding-agent/src/core/tools/bash.ts`, `read.ts`, `edit.ts`, `write.ts`, `grep.ts`, `find.ts`, `ls.ts`, `index.ts` | Builtin capability metadata on each definition factory (read/grep/find/ls → `filesystem.read`, edit/write → `filesystem.write`, the shared `createShellToolDefinition` factory for bash and powershell → `process.execute`); `index.ts` re-exports the permissions module | `core/tools/permissions.ts` | One literal field per factory, no behavior change; `index.ts` export block only adds re-exports; upstream's powershell tool (0.84.3) inherits `process.execute` from the shared shell factory, and the `minimal` preset's static deny+hide list includes it by name alongside bash/edit/write | `packages/coding-agent/test/suite/tool-permissions.test.ts`, `packages/coding-agent/test/profiles.test.ts` (pins the preset rule lists) |
| `packages/coding-agent/src/core/tools/permissions.ts` (issue #82) | Matcher hardening: `command` rules match at a whitespace token boundary (input equals the rule or continues with whitespace — `git` no longer matches `gitx evil`, `git push` no longer matches `git status; curl evil \| sh`), `path` rules normalize both sides before the segment-boundary check (leading `~` expands via `os.homedir()`, both resolve against the evaluation `cwd` the caller passes — the extension passes the session cwd — so relative args and `..` escapes resolve first), and empty-string matchers count as unspecified on all four fields (`tool`/`capability` previously never matched on `""`) | none (self-contained; stdlib `node:path`/`node:os` only) | Token-boundary fix deliberately leaves prefix-rule coarseness, documented in the module doc: an allow rule for `git` still covers `git status; <payload>` (deny/ask are the safe uses, containerization for real isolation); path resolution is lexical (no symlink following) and case-sensitive, as before | `packages/coding-agent/test/permissions.test.ts` |
| `packages/coding-agent/src/modes/interactive/interactive-mode.ts` | `/session` panel Tools line renders tool output volume and, when truncation happened, removed bytes and artifact count | none (display-only; formats via upstream `formatSize` from `core/tools/truncate.ts`) | Reads additive `SessionStats` fields only; truncation detail line appears only when `truncatedToolOutputBytes > 0`; no other panel behavior changes | `packages/coding-agent/test/suite/tool-output-bounds.test.ts`, `packages/coding-agent/test/agent-session-stats.test.ts` (fields the panel renders) |
| `packages/coding-agent/src/core/sdk.ts` | Interrupted-turn recovery wiring in `createAgentSession`: after the existing session context is built and before agent state adopts the messages, adjacency-repairable dangling tool calls get terminal error toolResults appended (issue #84: turns with later message entries are left unrepaired); the context is re-derived only when something was appended | `core/sessions/recovery.ts` | Runs only when the loaded session has messages (brand-new sessions untouched); repair is append-only through `SessionManager.appendMessage`, so persisted history is never rewritten and in-memory sessions resume repaired too | `packages/coding-agent/test/session-recovery.test.ts` |
| `.gitignore` | Ignore the `.fork-publish/` staging directory written by `scripts/fork-publish-github.mjs` | `scripts/fork-publish-github.mjs` | One appended ignore line under a new comment; no upstream pattern changed | Staging dir never appears in `git status` after `node scripts/fork-publish-github.mjs --auto 1 --dry-run` |
| `.github/workflows/fork-release.yml` | Fork-added release workflow: guard dispatch to `refs/heads/main`, verify the `v<version>` tag is free on the remote BEFORE any publish (a colliding version must fail while the run is retryable, not wedge a half-published release), publish to GitHub Packages, tag, build the standalone tarball, stamp the fork identity into `packages/coding-agent/package.json` (name `@r-dson/pi-standalone`, version = release version — the compiled binaries read the package.json next to the executable at runtime, so unstamped binaries would report the upstream version and `update --self` would classify them as the upstream npm package), compile the six platform binaries via `scripts/build-binaries.sh --skip-install --skip-build` (bun pinned to the same 1.3.14 as `build-binaries.yml`), smoke-test the linux-x64 binary's `--version` against the release version, then create the GitHub Release as a draft, upload `pi-fork.tgz` plus the six `pi-<os>-<arch>` archives to it, and only then publish + mark latest | `scripts/fork-publish-github.mjs`, `scripts/fork-standalone-package.mjs`, `scripts/build-binaries.sh` (invoked with skip flags; the script itself stays upstream-identical), `scripts/install.sh` (linked from release notes) | Fork-added file with no upstream counterpart, so upstream merges never conflict here; asset filenames are stable across releases so `releases/latest/download/...` keeps resolving, and the draft-then-promote order means they never resolve to a release without its assets; the identity stamp runs after fork-publish (whose `--auto` derives the version from the pristine workspace manifest) and after the standalone packager (which must read the pristine manifest), so neither sees the rewritten fields; the tag-free check uses `git ls-remote` because the shallow checkout carries no pre-existing tags locally; standalone staging wipes only `.fork-publish/standalone`, never the whole `.fork-publish` (the release-notes step reads `.fork-publish/summary.json`) | The workflow run itself (the binary smoke test gates it); `scripts/fork-standalone-package.test.mjs`; `scripts/install.test.mjs` covers the installer that consumes the assets |
| `scripts/install.sh` | Fork-owned installer, binary-first since 2026-09-13: `uname` maps darwin/linux x64/arm64 to a precompiled `pi-<platform>.tar.gz` release asset and installs it with only `curl` + `tar` (extracted wrapper dir swapped into `<prefix>/lib/pi-fork`, `bin/pi` a relative symlink `../lib/pi-fork/pi` — safe because the bun-compiled executable resolves its own real path for asset lookup — prefix default `~/.local`, `PI_INSTALL_PREFIX` overrides); every other case (unsupported platform, release without binary assets, `PI_INSTALL_METHOD=npm`) takes the npm tarball path unchanged from the fork's earlier script: preflight (curl, npm, Node >= 22.19), npm-prefix selection with version-manager exclusion, downgrade warning against the prefix's own install (issue #78), `npm install -g --ignore-scripts` from the downloaded `pi-fork.tgz`, startup smoke test with the single clean-retry from the fork.8 regression, PATH-resolution note, `--uninstall`; `PI_INSTALL_METHOD=binary|npm` forces a method, automatic mode falls back to npm when the binary download fails, and `--uninstall` dispatches on what `bin/pi` actually is (our symlink → remove it and `lib/pi-fork` without npm; npm-owned → npm uninstall; else refuse). Guards: a foreign `bin/pi` is refused with a move-aside hint; an npm-owned `bin/pi` blocks a binary install until `--uninstall` runs (method switches are explicit) | none (fork-owned script) | Fork-added file with no upstream counterpart, so upstream merges never conflict here; POSIX sh + `curl` + `tar` + `uname` + `npm` + `mktemp`, plus `readlink` for the two symlink-contract checks (not a POSIX utility, but universal on darwin/linux) (the comparator is a pure-awk semver subset: `x.y.z` with optional `-fork.N`, unparseable input fails open to no warning); the binary path never invokes npm or node — no Node.js requirement at all; hard failures die with the fix in the message and every optional decision is warning-only because the script runs under `curl \| sh` with no tty guarantee — no decision prompts, no animations or menus (deliberate divergence); colors only when stdout is a tty, curl draws its own meter on a tty; the latest path skips the downgrade check because every fork release is promoted to latest only after its assets are attached; no checksums by design (TLS-only is the documented posture). Upstream's pi.dev `install.sh` (unlicensed, not in their repo) was feature inspiration only — the 2026-09-13 rewrite was implemented clean-room by an agent that never saw it, working from a functional spec, with `scripts/install.test.mjs` (written first, against the spec) as the executable contract; no code copied, no Node bootstrap (the npm-path error points at nodejs.org), and no managed-install registration or update checks: the managed-update subsystem stays dropped per the decision above | `sh -n scripts/install.sh`; `node --test scripts/install.test.mjs` (13 stub-driven cases: binary install without npm, pinned URL, platform and 404 fallbacks, forced methods, invalid method exit 2, uninstall, foreign and npm-owned guards, downgrade warning, smoke-test failure); the release install path on every fork release |
| `README.md` | Fork-owned rewrite: fork identity, install methods, fork-feature docs, upstream credit; the upstream README is preserved verbatim at `docs/fork/upstream-README.md` | none (documentation) | README conflicts on every upstream merge are accepted; on sync, port relevant upstream README changes into the fork README manually, using the preserved copy as the reference | This README |
| `.github/workflows/build-binaries.yml` | Remove the `v*` tag push trigger (fork releases are cut by `fork-release.yml`; upstream's npm-publish/binary jobs cannot run here) | none (trigger-only change; manual `workflow_dispatch` remains) | Trigger-only change; job definitions untouched so upstream merges conflict at most on the `on:` block | The workflow no longer appears in fork release runs |
| `packages/coding-agent/src/modes/interactive/components/assistant-message.ts` | Hidden-thinking branch renders the newest thinking run as a live preview BLOCK (header line `hiddenThinkingLabel` + elapsed timer + expand hint, then the last `THINKING_PREVIEW_LINES` visual lines of the run via upstream's `truncateToVisualLines` in the width-lazy cached-render idiom from bash-execution.ts — newlines preserved, wrapped at the real render width, folded-away content above implied by the fade with no marker line, italic `thinkingText` styling; while the run is live the block reserves the full `THINKING_PREVIEW_LINES` height up front — content top-aligned, blank rows below — so it never grows mid-stream and nothing below it reflows as tokens arrive, and once the run ends it renders at natural height so short finished runs carry no blank rows; timer recomputed on each `message_update` while streaming; when the terminal answered the OSC 11 background query, the tail's words are individually colored between the terminal background and the thinking gray — position-keyed per word, so the oldest visible word sits at the background color exactly and words darken continuously as newer text arrives; without an endpoint the block stays uniformly gray) with the header switching from the live timer to the frozen `Thought for Ns` marker as soon as non-thinking content streams after the run or the message finishes (since 2026-09-06 the live header's ellipsis breathes: the label's trailing dots become a wall-clock-driven animated field — 350 ms frames, cycle full→empty→full with a hold at full, padded to 3 chars so the timer never shifts; a pure function of elapsed time recomputed on each streaming rebuild, no ticker, so a long gap between chunks freezes it like the timer already does) — the tail block stays under the header in both states and in rebuilds; superseded 2026-09-06 (maintainer: reasoning windows must always carry their header): a run ended by a TOOL CALL keeps its header frozen to `Thought for Ns`, and a continuation message's opening run renders its own live header — the `continuesAfterToolCall` threading (interactive-mode's `nextAssistantContinuesAfterToolCall`, tracked from `stopReason === "toolUse"` at `message_end`, and the component's constructor flag) is deleted; interactive-mode still threads the previous assistant's provider/model key (`lastAssistantModel`, reset at rebuild start) for the attribution marker, so a think-act-think cycle now shows a header per reasoning window: header+tail above the tool call, header+tail below (the residual #83 rule stands only within a single message, where older runs never render — pi ends a message at each tool call, so distinct windows are distinct messages); expand hint reuses `keyHint("app.thinking.toggle", "to expand thinking")`; the clock measures the newest run (run-count growth restarts it, including runs batched into one update with their end), and durations frozen from never-streamed runs are lower bounds; 2026-09-02 (#135/#136): the component takes an optional `previousModel` key and renders a muted model-id line above the content when this message's model differs from the previous assistant's — mixed-model (handoff) sessions show who said what; omitted key renders nothing, so single-model sessions and all pre-existing call sites are byte-identical | none (display-only, component-local) | Interactive-mode wiring unchanged: `message_update`/`message_end` already call `updateContent(msg, isStreaming)`. Duration comes from in-component timing frozen at the first non-thinking block after the newest run (a newest run that trails to message end is measured to the end), so it is approximate and only exists for messages streamed live; ctrl+t keeps the live component instances and their duration state (`setHideThinkingBlock`), so live-streamed messages keep the marker — the static-label fallback applies to real rebuilds (`rebuildChatFromMessages` on settings changes while idle); `setHiddenThinkingLabel` extension API keeps its meaning as preview prefix and fallback marker | `packages/coding-agent/test/assistant-message.test.ts` (hidden thinking preview describe); 2026-09-13: the tail-block machinery (fixed-height window, per-word fade, breathing dots, OSC 11 background holder) moved verbatim to `components/preview-window.ts` (`PREVIEW_WINDOW_LINES`, `createPreviewWindow`, `setPreviewFadeBackground`) and this component now calls `createPreviewWindow` — extracted when the task tool's sub-agent windows (its row) adopted the same look; behavior identical |
| `packages/coding-agent/src/core/provider-attribution.ts` | Telemetry purge (issue #32): default attribution headers (OpenRouter `HTTP-Referer`/`X-OpenRouter-Title`/`X-OpenRouter-Categories`, NVIDIA `X-BILLING-INVOKE-ORIGIN`, Cloudflare `User-Agent: pi-coding-agent`) and the `isInstallTelemetryEnabled` gate deleted; `mergeProviderAttributionHeaders` drops its `settingsManager` parameter | none (fork-local policy) | All removed headers are optional app identification, not provider API requirements (OpenRouter docs mark referer/title optional; NVIDIA's header is undocumented billing attribution; the pi app User-Agent is removed from all provider APIs in packages/ai; providers reached through the OpenAI SDK still send that SDK's own default UA (`OpenAI/JS x.y.z`), which is library-generic, not app- or install-identifying). The OpenCode `x-opencode-session`/`x-opencode-client` headers stay: the Zen gateway serves anonymous (header-less) requests from a heavily restricted rate-limit tier (upstream earendil-works/pi#2824), so they are the provider-required minimum; 2026-09-05: `openRouterAttributionHeaders` re-added as opt-in (fork values, exported for sdk.ts's `providerAttribution`-gated call) | `packages/coding-agent/test/sdk-openrouter-attribution.test.ts` |
| `packages/coding-agent/src/modes/interactive/theme/theme.ts` | Additive `Theme.fgValue(color)` getter returning the resolved foreground value (`#rrggbb` hex or 256 index) alongside a private parallel `fgRawValues` map filled in the constructor loop | `modes/interactive/components/preview-window.ts` (preview gray RGB for the fade; was assistant-message.ts before the 2026-09-13 extraction) | The public API is read-only and additive; the raw map duplicates values already iterated in the constructor, no behavior change; themes defining colors as palette indices instead of hex get no fade (uniform preview) | `packages/coding-agent/test/assistant-message.test.ts` (fade describe) |
| `packages/coding-agent/src/modes/interactive/interactive-mode.ts` (fade hookup only) | `init()` queries the terminal background once via pi-tui's `queryTerminalBackgroundColor` (OSC 11, 300 ms timeout) and forwards the RGB (or undefined) to `setPreviewFadeBackground` (was `setThinkingPreviewFadeBackground` before the 2026-09-13 preview-window.ts extraction); the call is wrapped so a TUI that is not yet live leaves the preview uniform | `modes/interactive/components/assistant-message.ts` | Same TUI query machinery the theme controller already uses; print/RPC/json modes never run `init()` here; note this file also carries the fork's tool-preview rebuild hook (separate row) | Startup smoke: interactive mode boots with the query wired |
| `packages/coding-agent/src/modes/interactive/components/footer.ts` | Footer stats spell out the cache numbers instead of single letters: `R`/`W`/`CH%` became `Read`/`Write`/`Cache %` (the `↑`/`↓` arrows stay); 2026-09-06: `setTransientStatus(message, requestRender)` shows a message in place of the pwd line for 3 s (sanitized, truncated, timer reset on repeat calls, cleared on `dispose`), wired to the ctrl+t thinking toggle in interactive-mode.ts so ephemeral UI feedback never enters the chat status channel — `showStatus` replaces the previous chat status, which clobbered an extension's run summary emitted via `ctx.ui.notify`; 2026-09-11: context usage renders as `25k/200k (12.3%)` instead of `12.3%/200k` (tokens and window alongside the percent; `?/200k` when tokens are unknown, e.g. right after compaction); 2026-09-12: the cache diagnostics (`Read`/`Write`/`Cache %`) moved after the context usage — the line reads traffic (in/out/cost), context, then cache detail — and the `usageDisplay` setting (`"minimal"` fork default, `"all"` restores the raw totals) drops the cumulative cache read/write counts, keeping the hit rate; the setting lives in settings-manager.ts, is read live in `render` so `/settings` changes apply immediately, and the `/settings` row ships via settings-selector.ts | `core/settings-manager.ts` (the `usageDisplay` field + getter/setter) | Stats semantics unchanged; the transient message replaces only the first footer line and expires back to the pwd line; transcript-worthy events intentionally stay in the chat | `packages/coding-agent/test/footer-width.test.ts` (transient status, context usage display, and usage detail describes) |
| `packages/coding-agent/src/core/tools/renderers/read.ts`, `core/tools/grep.ts` | Counts-first collapsed summaries (renderer changes): `read`'s collapsed success result renders `N lines (ctrl+o to expand)` instead of nothing (line count after trailing-empty trim, computed from the raw text so the collapsed path never syntax-highlights; image-only results stay silent), and `grep`'s result leads with `N matches` — execute stamps the true count into `details.matchCount` (context lines and the trailing notices block excluded), a notices-stripped line count is the fallback for results without details, and error results carry no count. Since the 2026-09-03 sync, read's delta lives in upstream's extracted `renderers/read.ts` module (`read.ts` spreads `readRenderers` and no longer defines renderers inline); since 2026-09-06 (code-review dedup) grep's delta lives in `renderers/grep.ts` the same way (`grep.ts` spreads `grepRenderers` instead of carrying inline fork copies that shadowed the module — the shadow left renderer-only presentations such as mini/tui and client-tui-chat on the old count-less format and duplicated `formatGrepCall` byte-for-byte) | none (display-only) | `read`'s expanded and error paths byte-identical; `grep` prepends the count in both collapsed and expanded views; both reuse the repo's `keyHint("app.tools.expand")` hint pattern; heavier presentation (grouping, dots, output modes) stays in pi-claude-code-ui per the placement decision | `packages/coding-agent/test/tool-previews.test.ts` (summary, empty and image-only reads, details count, notices fallback, error skip) |
| `packages/coding-agent/src/modes/interactive/components/tool-execution.ts`, `packages/coding-agent/src/modes/interactive/interactive-mode.ts` (rebuild hook only) | Elapsed ticker on running tool rows: `markExecutionStarted` starts a 1s `ui.requestRender` interval, `render` appends a muted `Elapsed Ns` line (whole seconds) while the row has no result, any `updateResult` (including streaming partials) clears the interval, and `destroy()` — called by `rebuildChatFromMessages` over `pendingTools` before the rebuild replaces running rows — stops the ticker plus any renderer-owned interval in shared state (bash's own ticker), so a mid-run rebuild leaks nothing | none (display-only, component-local + a destroy call) | Tools with their own elapsed line take over from their first partial (bash renders `Elapsed` in tenths once output streams, so the generic ticker covers the silent pre-output window and hands over); `requestRender` coalesces with a 16ms floor, so N parallel ticking rows still cost at most ~1 render per second; the line renders outside the content box so it applies to both default and self-render shells (a self-render row that renders zero content lines early-returns without the elapsed line) | `packages/coding-agent/test/tool-previews.test.ts` (ticker tick, clear on result, bash handover, destroy without result) |
| `packages/ai/src/api/openai-completions.ts`, `packages/ai/src/api/openai-responses.ts`, `packages/ai/src/api/openai-codex-responses.ts`, `packages/ai/src/api/google-generative-ai.ts`, `packages/ai/src/api/google-vertex.ts`, `packages/ai/src/api/azure-openai-responses.ts`, `packages/ai/src/api/mistral-conversations.ts` (`anthropic-messages.ts` carries its own row above for the `onWireRewrite` seam), `packages/ai/src/utils/pi-user-agent.ts` (deleted) | Telemetry purge (issue #32): remove the app `User-Agent: pi (...)` from every provider request; since 2026-09-05 `openai-completions.ts` `parseChunkUsage` also maps vLLM's `prompt_tokens_details.created_cache_tokens` as a cacheWrite source alongside OpenRouter's `cache_write_tokens` (see `docs/fork/cache-usage-research.md`); 2026-09-05: transformHeaders passes `openRouterAttributionHeaders(model)` when the opt-in `providerAttribution` setting is enabled | none | Header removal only; auth/api headers untouched; `packages/ai/src/auth/oauth/github-copilot.ts` keeps `GitHubCopilotChat/0.35.0` because that API requires the client UA | `packages/ai/test/azure-openai-base-url.test.ts`, `packages/ai/test/openai-completions-usage-cache-fields.test.ts`; UA-null assertion updates (telemetry fallout, recorded 2026-09-13) in `google-raw-stop-reason.test.ts`, `google-vertex-api-key-resolution.test.ts`, `mistral-http-transport.test.ts`, `xai-responses.test.ts`, `openai-codex-stream.test.ts` |
| `packages/coding-agent/src/core/telemetry.ts` (deleted), `packages/coding-agent/src/utils/version-check.ts` (deleted), `packages/coding-agent/src/utils/pi-user-agent.ts` (deleted) | Telemetry purge (issue #32): install-telemetry gate, pi.dev latest-version fetch (incl. semver helpers and pi UA builder) had no remaining callers | none (fork-local policy) | Deleting beats neutralizing here: every call site (install ping, startup version check, self-update plan, catalog UA) is also purged in this issue, so upstream merges re-add whole files that the fork deletes again mechanically | `packages/coding-agent/test/suite/no-tracking-traffic.test.ts` |
| `packages/coding-agent/src/core/sdk.ts` | Telemetry purge (issue #32): `mergeProviderAttributionHeaders` call updated for the dropped `settingsManager` parameter | `core/provider-attribution.ts` | Header merge semantics otherwise unchanged (provider/request sources still override) | `packages/coding-agent/test/sdk-openrouter-attribution.test.ts` |
| `packages/coding-agent/src/core/settings-manager.ts` | Telemetry purge (issue #32): `enableInstallTelemetry`, `enableAnalytics`, and `trackingId` settings plus getters/setters removed (no dead `telemetry: false` keys left behind); 2026-09-05: `updateCheck`/`providerAttribution` settings plus getters/setters added (default false, the opt-in phone-home decision above) | none (fork-local policy) | `enableAnalytics`/`trackingId` had no consumers outside settings-manager (tracking identifier stored but never transmitted); existing settings files with the keys keep loading — unknown keys are ignored by the existing merge logic | `packages/coding-agent/test/settings-manager.test.ts`, `packages/coding-agent/test/first-time-setup.test.ts` |
| `packages/coding-agent/src/core/remote-catalog-provider.ts` | Telemetry purge (issue #32): pi.dev overlay fetch removed — `withRemoteCatalog`'s `refreshModels` restores only the persisted local models-store overlay and never touches the network; `catalogBaseUrl` parameter, etag/304/TTL revalidation, and `REMOTE_CATALOG_REFRESH_INTERVAL_MS` deleted | none (fork-local policy) | `allowNetwork`/`force` are now inert for catalogs (callers unchanged); `pi update --models` degrades to a local re-restore and since issue #84 says so ("Model catalogs restored from the local store (network refresh unavailable in this fork)") instead of implying a network refresh; upstream merges conflict only inside this one fork-neutralized file plus that one package-manager-cli.ts message/help line | `packages/coding-agent/test/remote-catalog-provider.test.ts` |
| `packages/coding-agent/src/core/model-runtime.ts` | Telemetry purge (issue #32): `CreateModelRuntimeOptions.catalogBaseUrl` removed (pointed at the purged catalog fetch); builtin wrap becomes `withRemoteCatalog(provider, builtinModelDataGeneratedAt)` | `core/remote-catalog-provider.ts` | No other caller passed `catalogBaseUrl`; runtime refresh flow (local restore + provider availability) unchanged | `packages/coding-agent/test/remote-catalog-provider.test.ts`, `packages/coding-agent/test/suite/no-tracking-traffic.test.ts` |
| `packages/coding-agent/src/modes/interactive/interactive-mode.ts` | Telemetry purge (issue #32): `reportInstallTelemetry` (pi.dev/api/report-install ping), startup `checkForNewPiVersion` call, `checkForPackageUpdates` (automatic npm/git extension-update check), `showNewVersionNotification`, `showPackageUpdateNotification`, and their imports removed; 2026-09-05: opt-in update check re-added to the startup block (gated on the `updateCheck` setting, PI_OFFLINE respected) | none (fork-local policy) | Local changelog display (`getChangelogForDisplay`) survives minus the pings; startup catalog refresh call stays (now local-only via the neutralized wrapper); `pi update --extensions` still performs explicit update checks | `packages/coding-agent/test/suite/no-tracking-traffic.test.ts`, `packages/coding-agent/test/suite/regressions/7829-invalid-settings-warning.test.ts` |
| `packages/coding-agent/src/modes/interactive/components/settings-selector.ts` | Telemetry purge (issue #32): "Install telemetry" toggle, config field, callback, and case removed; 2026-09-05: "Update check" and "Provider attribution" toggles added (default off); 2026-09-12: "Usage stats" values row (minimal/all) for the footer's `usageDisplay` setting | none (fork-local policy) | Display-only change; no other setting affected | `packages/coding-agent/test/settings-manager.test.ts` |
| `packages/coding-agent/src/modes/interactive/components/first-time-setup.ts`, `packages/coding-agent/src/cli/startup-ui.ts` | Telemetry purge (issue #32): analytics opt-in step removed; first-time setup is theme-only and no longer writes `enableAnalytics`/`trackingId`; 2026-09-05: wizard gains a privacy step (`updateCheck`/`providerAttribution` questions, Off preselected) and the fork takes over the gate — no `PI_EXPERIMENTAL` requirement, runs once per install (fresh, plus existing installs lacking both privacy keys; skipping records the decline; the theme question is always first, preselected to Automatic so confirming without navigating adopts it (superseded for installs with a set theme 2026-09-06: the theme step is skipped entirely — see the wizard theme-skip row); 2026-09-06: the theme list is `["/", ...getAvailableThemes()]` — "Automatic" (rendered label for the `/` setting, the same value the /settings theme submenu uses) is first and the fresh-install default, resolving to the detected terminal appearance at every startup; the wizard's preview callback maps `/` to the detected appearance for live preview) | none (fork-local policy) | `FirstTimeSetupResult` drops `shareAnalytics`; `FirstTimeSetupResult` now carries `updateCheck`/`providerAttribution` and `theme` is a plain string (may be `/`); the gate no longer consults `areExperimentalFeaturesEnabled` | `packages/coding-agent/test/first-time-setup.test.ts`, `packages/coding-agent/test/first-time-setup-fork.test.ts` |
| `packages/coding-agent/src/cli/args.ts` | Telemetry purge (issue #32): `PI_TELEMETRY` help line removed (env var no longer read) | none (fork-local policy) | Help text only; no parser change | `packages/coding-agent/test/args.test.ts` |
| `packages/coding-agent/src/main.ts` | Telemetry purge (issue #32): offline mode no longer sets `PI_SKIP_VERSION_CHECK` (env var no longer read anywhere) | none (fork-local policy) | `PI_OFFLINE=1` still disables startup network operations | `packages/coding-agent/test/suite/no-tracking-traffic.test.ts` |
| `packages/coding-agent/src/package-manager-cli.ts` | Telemetry purge (issue #32): `pi update --self` no longer consults pi.dev/api/latest-version; the plan statically installs `PACKAGE_NAME@latest` via npm and the pi.dev-provided update note is gone (issue #29 rebuilds update UX) | none (fork-local policy) | Self-update mechanics (install-method detection, npm/pnpm commands, Windows quarantine) unchanged; npm resolves `@latest` against the registry only when the user runs the command; the pi.dev package-rename redirect flow is dropped with the fetch | `packages/coding-agent/test/package-command-paths.test.ts` |
| `packages/coding-agent/src/package-manager-cli.ts` | Fork-aware self-update (issues #29/#74): the update branch classifies the running install via `core/self-update-source.ts`; fork-standalone installs pass the fork tarball URL as `installSpec` (success message "latest fork release", failure/unavailable paths add the install.sh reinstall hint), fork-registry installs (`@r-dson/*` other than the standalone package, i.e. GitHub Packages) pass `@r-dson/pi-coding-agent@latest` so the user's .npmrc scope mapping resolves the fork registry (success message "latest fork registry release"), upstream-npm installs print a channel notice before the regular update, from-source/unknown installs keep the existing unavailable message | `core/self-update-source.ts` | installSpec swap only — all existing plumbing (install-method detection, prefix inference, managed/writable guards, Windows npm quarantine, pnpm hint, fallback printing) is reused unchanged; notice is gated on `installMethod !== "unknown"` so from-source checkouts (which also carry the upstream package name) are not told they are upstream installs; the fork-standalone tarball installSpec additionally picks up npm >= 12's `--allow-remote=all` from the conditional in config.ts's npm command builder | `packages/coding-agent/test/package-command-paths.test.ts` |
| `packages/coding-agent/src/core/self-update-source.ts` | Fork-added module (issues #29/#74): channel constants (standalone/fork-registry/upstream package names, fork tarball URL, install.sh command), `classifySelfUpdateInstall` (`@r-dson/pi-standalone` → fork-standalone, any other `@r-dson/*` → fork-registry, `@earendil-works/pi-coding-agent` → upstream-package, rest → other), and `resolveRunningPackageName` | none (fork-owned file) | Fork-added file with no upstream counterpart, so upstream merges never conflict here; the package name is read fresh from `getPackageDir()`'s package.json so `PI_PACKAGE_DIR` overrides apply (test seam + Nix-style installs); an unreadable/missing manifest resolves to "" and the CLI refuses to self-update rather than guessing a channel | `packages/coding-agent/test/self-update-source.test.ts` |
| `packages/coding-agent/src/config.ts` | Conditional `--allow-remote=all` in the npm self-update command builder (issue #74): when the installSpec is a remote tarball URL, the running npm's version is probed once through the configured npmCommand and the flag is appended only for parsed major >= 12; `parseNpmMajorVersion`/`npmAllowRemoteArgsForInstallSpec` are exported pure helpers. Branding (2026-09-06): the `APP_TITLE` fallback changed `π` → `Pi Fork` (terminal title display only) | none (spec-string-local; the fork channel choice happens in package-manager-cli.ts) | npm >= 12 blocks remote-tarball install specs by default (EALLOWREMOTE) while npm < 12 errors on the unknown option, so both the URL gate and the version gate are required; a failed or unparseable version probe fails open (no flag) so old npm keeps working; registry-spec installs never trigger the version probe | `packages/coding-agent/test/config.test.ts` (allow-remote flag unit tests + versioned fake-npm command shape) |
| `packages/coding-agent/docs/settings.md`, `packages/coding-agent/docs/environment-variables.md`, `packages/coding-agent/docs/sdk.md`, `packages/coding-agent/README.md`, `packages/coding-agent/docs/models.md` | Telemetry purge (issue #32): telemetry/update-check sections rewritten for the fork's zero-telemetry policy; `enableInstallTelemetry`/`enableAnalytics`/`trackingId` setting rows, `PI_TELEMETRY`/`PI_SKIP_VERSION_CHECK` env rows, and the pi.dev catalog-refresh SDK comment removed. Issue #80 adds fork-feature documentation: settings.md's Tools row for `tools.maxToolOutputBytes` and sdk.md's Compaction section documenting the required `SummarizationPrefix` with an example call; #139/#140 add models.md's mapless-semantics paragraph (no map means `xhigh` offered and provider-arbitrated, `max` clamps to `xhigh`, `"xhigh": null` hides); the `tools.permissions` row was removed with the core mode (issue #70), and settings.md's Tools section points at the permission-policies extension instead | none (documentation) | Docs-only; on upstream sync, port these sections from the fork-policy text rather than re-adding the upstream telemetry documentation, and keep the fork-feature sections (the settings they document are fork-only) | The doc build/lint (`npm run check`) |
| `packages/coding-agent/src/core/compaction/compaction.ts` | Prefix-replaying compaction summarizer (issue #40, cache plan phase A): `generateSummaryWithUsage`/`generateSummary`/`compact` take a required `SummarizationPrefix` (agent system prompt + tools, new position after `model`) and build the request as `{systemPrompt, tools, [...convertToLlm(currentMessages), appended user instruction]}`; persona + checkpoint format + `Additional focus` fold into the instruction text, and `previousSummary` selects the update-prompt variant of that text while the old summary itself is replayed as the checkpoint message heading `replayMessages` (see the update-compactions decision row); split-turn `generateTurnPrefixSummary` keeps its `buildSummarizationContext`/`serializeConversation`/`SUMMARIZATION_SYSTEM_PROMPT` standalone path (still has callers), and when the cut splits the first kept turn of a prior checkpoint with no new messages to summarize, the history section is seeded with `previousSummary` verbatim so the new checkpoint keeps the old context instead of replacing it with "No prior history." | none (compaction-local; DSH idea reimplemented natively) | `completeSummarization` choke point unchanged (maxTokens cap, thinkingLevel, retry); `toolChoice` is never set — upstream removed `toolChoice: "none"` (6b36eb592, #8649/#8638: some providers reject it), so replayed tools stay uncallable through the post-hoc rejection of tool-call responses in `generateSummaryWithUsage`/`generateTurnPrefixSummary`; checkpoint output format body byte-identical so downstream consumers (compaction entry, context rebuild) do not change; `serializeConversation`/`SUMMARIZATION_SYSTEM_PROMPT` kept for the turn-prefix path, extension examples, and the public API export | `packages/coding-agent/test/suite/agent-session-compaction.test.ts` (prefix-replay acceptance + split-turn standalone), `packages/coding-agent/test/compaction-summary-reasoning.test.ts` (split-turn previous-checkpoint carry-through), `packages/coding-agent/test/compaction.test.ts` |
| `packages/coding-agent/src/core/compaction/branch-summarization.ts` | Prefix-replaying branch summary (issue #40, cache plan phase A): `GenerateBranchSummaryOptions` gains a required `prefix: SummarizationPrefix`; the request replays the agent system prompt + tools + `convertToLlm` branch messages plus one appended user turn carrying the persona and branch instructions (`<conversation>` serialization dropped from this path). Replay messages keep tool-call pairing valid (issue #72): tool results are included, each assistant-with-calls message forms one budget unit with its trailing results so the token walk cannot split a pair, a dangling call at a branch tail gets a synthesized terminal result (recovery.ts's `INTERRUPTED_TOOL_RESULT_TEXT`), and an orphan result whose call fell outside the window is dropped; entry-to-message conversion delegates to the projector's `sessionEntryToContextMessages`, so the replay mirrors the regular context projection exactly (null-content hardening, empty-summary skipping) instead of re-implementing the switch | `core/compaction/compaction.ts` (`SummarizationPrefix`, `completeSummarization`) | `replaceInstructions` still replaces only the branch instruction body — the persona line is always prepended; unit-granular budget cuts are coarser than the old per-message cuts, which only trims how much of an over-budget branch fits, never correctness | `packages/coding-agent/test/branch-summarization.test.ts` (pairing + dangling-call tests) |
| `packages/coding-agent/src/core/compaction/replay.ts`, `packages/coding-agent/src/core/compaction/branch-units.ts` (fork-added) | Replay extraction (2026-09-20, from the placement-hardening round): every byte the replaying summarizers send now lives in fork-owned modules — the checkpoint-format prompt constants and `buildSummaryInstruction`, the shared `buildReplaySummarizationContext` (compaction and branch summarization previously carried two structurally identical inline copies), `buildStandaloneSummarizationContext`, `buildReplayMessages`, `SummarizationPrefix` (still re-exported from compaction.ts), and the branch replay units (`groupBranchUnits`, projector-delegating entry conversion, dangling-call repair). Upstream-owned compaction.ts/branch-summarization.ts keep upstream's function structure and delegate — the remaining hunks are parameter lists and call-site changes rather than rewritten bodies, and fork-authored insertions in the pair drop 271 to 111 — but the move is conflict-neutral, not conflict-reducing (hunk count 29 to 27; a deletion hunk collides with an upstream edit exactly as an in-place edit does), so its justification is the deduplication plus the digest gate, not fewer merge conflicts | none (fork-owned files) | The golden digest test (`test/compaction-replay-golden.test.ts`) pins the request bytes across the move and any future change; on upstream syncs, upstream edits to the moved prompt constants or summarizer bodies conflict against the delegation seams — resolve by porting the upstream change into replay.ts/branch-units.ts and updating the golden digests only when the request bytes legitimately change | `packages/coding-agent/test/compaction-replay-golden.test.ts`, `packages/coding-agent/test/branch-summarization.test.ts`, `packages/coding-agent/test/suite/agent-session-compaction.test.ts` |
| `packages/coding-agent/src/core/agent-session.ts` | Compaction/branch-summary call sites (issue #40, cache plan phase A) pass `{systemPrompt: this.agent.state.systemPrompt, tools: this.agent.state.tools}` into `compact()`/`generateBranchSummary()` so the summarizer replays the same prefix the last regular request used | `core/compaction/compaction.ts`, `core/compaction/branch-summarization.ts` | `agent.state` is the source the regular turn's request is built from, so reading it at compaction time yields the same content; no other behavior changes | `packages/coding-agent/test/suite/agent-session-compaction.test.ts` |
| `packages/coding-agent/src/index.ts` | Export the `SummarizationPrefix` type (issue #40) so SDK consumers can name the now-required compaction summarizer argument | `core/compaction/compaction.ts` | Type-only export-list addition, one line; no runtime change | Typecheck (`npm run check`) |
| `packages/coding-agent/src/core/agent-session.ts` | Runtime prefix-stability monitor (issue #41, cache plan phase B): wiring only since issue #60 — the observation mechanics (the transparent `agent.streamFunction` wrapper, the per-request diff driver, attribution recording) and `unwrapStreamFn` live in `core/sessions/request-observer.ts`, on the pure serialize/diff helpers in `core/sessions/prefix-stability.ts`; agent-session.ts keeps the constructor install, `expectInvalidation` announcements at the legitimate rewrite sites (`setModel`/model cyclers, `setActiveToolsByName` when the visible name list changes, extension prompt override apply/revert and the extension-resource prompt rebuild, before the default compaction summarizer call plus after both compaction state resets, before branch-summary generation plus after the navigateTree state reset), `unwrapStreamFn` in `_getSummarizationRequestAuth`'s default-stream detection, and `getSessionStats`'s `prefixInvalidationsByCause`; unannounced invalidations emit the additive `prefix_invalidated` `AgentSessionEvent` | `core/sessions/request-observer.ts`, `core/sessions/prefix-stability.ts` | Monitor failures are swallowed (a diagnostic must never block a request); model identity (provider+id) is compared alongside the prefix bytes because provider prompt caches are per model; the diff's stable case is proven by one byte-prefix comparison (`next.startsWith(previous.slice(0, -2))` — `messages` is the last serialized field), falling back to the structural diff only to attribute a mismatch, with the same result either way; counters are in-memory (run-scoped, reset on resume) like the tool-output diagnostics; announced expectations stay armed until the next stable request; the new event type is additive for the TUI/RPC/json event paths (their switches are non-exhaustive) | `packages/coding-agent/test/prefix-stability.test.ts`, `packages/coding-agent/test/suite/prefix-monitor.test.ts` |
| `packages/coding-agent/src/core/agent-session.ts` | Cache-economics attribution (issue #42, cache plan phase C): wiring only since issue #60 — kind claiming (one-shot retry marker, then an active summarizer window, else `turn`) and usage recording moved to `core/sessions/request-observer.ts` with the streamFn wrapper; agent-session.ts keeps `withRequestKind` windows around the default `compact()` and `generateBranchSummary()` calls, retry markers in `_prepareRetry` (before the continuation that re-issues the failed turn), `_runAutoCompaction`'s willRetry branch (overflow compact-and-retry replay), and the summarization `onRetryAttemptStart` callback, plus `getSessionStats`'s run-scoped `cacheUsageByKind` | `core/sessions/request-observer.ts`, `core/sessions/cache-usage.ts` | Usage is observed by attaching to the returned stream's `result()` promise, which resolves for done and error alike (failed/aborted requests count with their reported, usually zero, usage so request counts stay honest); the wrapper stays transparent on the wire (`unwrapped` marker intact); counters are in-memory run-scoped like the phase-B counters, and recording failures are swallowed (a diagnostic must never block a request); the first regular request after compaction attributes to `turn`, not the still-armed compaction expectation | `packages/coding-agent/test/suite/cache-stats.test.ts`, `packages/coding-agent/test/cache-usage.test.ts`, `packages/coding-agent/test/agent-session-stats.test.ts` |
| `packages/coding-agent/src/modes/interactive/interactive-mode.ts` | `/session` panel cache block (issue #42): run hit rate, read/written/uncached totals, indented per-kind lines for non-turn kinds, and a prefix-invalidation count line, rendered after the Tokens section | `core/sessions/cache-usage.ts` (`cacheHitRate`, `NON_TURN_REQUEST_KINDS`) | Reads additive `SessionStats` fields only; the block is hidden until this run's first provider request; headline uses one-decimal and per-kind lines integer percentages from the shared `cacheHitRate` (same denominator as the existing Tokens hit rate: read over read + written + uncached) | `packages/coding-agent/test/agent-session-stats.test.ts` (fields the panel renders), `packages/coding-agent/test/cache-usage.test.ts` |
| `packages/coding-agent/src/index.ts` | Export the `RequestKind` and `CacheUsageTotals` types (issue #42) so SDK consumers can name the new `SessionStats.cacheUsageByKind` field | `core/sessions/cache-usage.ts` | Type-only export-list addition, one line; no runtime change | Typecheck (`npm run check`) |
| `packages/coding-agent/src/core/package-manager.ts` | Canonical ordering for auto-discovered resources (issue #49): `collectAutoExtensionEntries` sorts path-keyed groups by resolved path (a package is one group holding its manifest-declared entry order, #139/#140; the earlier flattened sort reordered manifest entries); `collectSkillEntries` sorts its final entry list by path; both use a new local `comparePaths` code-unit comparator that `expandPackageGlob`'s pre-existing deterministic sort also now uses | none (package-manager-local; DSH idea reimplemented natively) | Sorting happens at collection, before the `toResolvedPaths` precedence-rank stable sort, so within-rank order becomes machine-independent while rank order, settings-declared order (packages array order, settings `extensions`/`skills` entries), settings-declared package-manifest entry order, and builtin tool order are unchanged (a manifest under an auto-discovered package sorts by path like other auto-discovered entries); a settings entry pointing at a directory enumerates that directory sorted (its children never had a meaningful order); one-time reorder on upgrade costs a single first-request cache miss per session | `packages/coding-agent/test/package-manager-canonical-order.test.ts` (reversed-readdir simulation), `packages/coding-agent/test/package-manager.test.ts` (canonical ordering describe), golden/stable-prefix suites (`test/system-prompt.test.ts`, `test/suite/prompt-stable-prefix.test.ts`); 2026-09-13 (binary-install follow-up): `mapPackageManagerSpawnError` maps a spawn ENOENT at the three npm seams (`runNpmCommand`, `runNpmCommandSync`, `getLatestNpmVersion`'s `npm view`) to a setup hint — pi and extensions placed under `~/.pi/agent/` run without a package manager, so the error names the options (install Node.js, point the `npmCommand` setting at bun/pnpm, place package files manually) instead of surfacing raw `spawn npm ENOENT`; git spawn paths untouched. `runCommandSync` now rethrows the spawn error itself instead of wrapping it, matching the async `runCommand` path, which has always rejected with the raw spawn error (`child.on("error", reject)`), so ENOENT (and any other spawn error) propagates uniformly to the mapping | `packages/coding-agent/test/package-manager.test.ts` (npmCommand describe: missing-manager install rejects with the hint) |
| `packages/coding-agent/src/core/agent-session.ts` | Session routing id on cache-replay summarizer calls (issue #51): `_runDefaultCompaction` passes `this.agent.sessionId` into `compact()` and the `generateBranchSummary()` call passes it as `sessionId` | none (routing-value pass-through; DSH parity) | `agent.sessionId` is the routing id regular requests carry (sdk.ts wires it from `sessionManager.getSessionId()` at agent construction; the agent loop spreads it into every regular request's options), so reading it at call time yields the same value; manual `compact()`, threshold, and overflow compaction all funnel through `_runDefaultCompaction`, so one site covers every path; the id is forwarded only — never minted | `packages/coding-agent/test/suite/agent-session-compaction.test.ts` (prefix-replay routing-id assertions), `packages/coding-agent/test/branch-summary-extensions.test.ts` (branch-summary call site) |
| `packages/coding-agent/src/core/compaction/compaction.ts` | Routing id forwarded only to the replaying history-summary call (issue #51): `compact()`'s existing `sessionId` parameter now carries the session routing id into `generateSummaryWithUsage`; `generateTurnPrefixSummary` drops its (now dead) `sessionId` parameter | none (compaction-local) | The split-turn turn-prefix summary is a standalone cache-opt-out request: it must NOT carry the routing id (the session's cache bucket stays untouched; `completeSummarization` keeps minting a throwaway id — its cacheOptOut logic is unchanged), pinned by the standalone split-turn suite test | `packages/coding-agent/test/suite/agent-session-compaction.test.ts` (standalone split-turn test asserts the routing id is absent) |
| `packages/coding-agent/src/core/compaction/branch-summarization.ts` | Optional `sessionId` on `GenerateBranchSummaryOptions` (issue #51), threaded like the other request options and included in the `completeSummarization` request options | `core/compaction/compaction.ts` (`completeSummarization`) | Optional field: existing callers compile and behave unchanged (no id forwarded when omitted); the replaying call inherits provider caching (`cacheOptOut=false`) so the id flows to the provider's session-scoped routing keys | `packages/coding-agent/test/branch-summarization.test.ts` (routing-id threading), `packages/coding-agent/test/branch-summary-extensions.test.ts` |
| `packages/coding-agent/src/core/messages.ts` | Image-placeholder rewrite extracted verbatim from sdk.ts's inline closure as `replaceImagesWithPlaceholders` (issue #53) | none (pure extraction) | Byte-identical rewrite (image parts become the deduped "Image reading is disabled." text, applied to user and toolResult messages including already-sent ones); exported so the SDK assembly and the test harness apply the identical filter instead of the tests re-implementing it | `packages/coding-agent/test/suite/prefix-monitor.test.ts` (settings-change describe), `packages/coding-agent/test/block-images.test.ts` (setting semantics) |
| `packages/coding-agent/src/core/sdk.ts` | `convertToLlmWithBlockImages` delegates its map body to `replaceImagesWithPlaceholders` (issue #53) | `core/messages.ts` | Gate and semantics unchanged: the setting is still read per request ("mid-session changes take effect") and the rewrite still applies to all messages | `packages/coding-agent/test/suite/prefix-monitor.test.ts` (harness exercises the same shared rewrite) |
| `packages/coding-agent/test/suite/harness.ts` | Harness `convertToLlm` mirrors the production assembly (issue #53): gate on `settingsManager.getBlockImages()` per request and apply `replaceImagesWithPlaceholders` when blocked | none (test utility; `core/messages.ts`) | Default `blockImages=false` keeps every existing harness caller byte-identical (the gate returns the converted messages unchanged); mid-session `setBlockImages` toggles now reach the provider context exactly as in real sessions | `packages/coding-agent/test/suite/prefix-monitor.test.ts` (settings-change describe), full suite run |
| `packages/coding-agent/src/core/agent-session.ts` | blockImages flip detection (issue #53), moved with the request observer to `core/sessions/request-observer.ts` (issue #60): the observer's `_lastBlockImages` field tracks the last-seen effective `getBlockImages()` value (read through the injected getter), and a flip between observed requests calls `expectInvalidation("settings-change")` before the prefix diff in the same observation | `core/sessions/request-observer.ts`, `core/sessions/prefix-stability.ts` (`settings-change` added to `PrefixInvalidationCause`; `PrefixInvalidationExpectation` derives from it via `Exclude`) | The first observed request only initializes the tracked value (no announcement); the announcement precedes the diff so the history rewrite the toggle triggers attributes to the setting instead of `unexpected-history-change`; a flip that leaves the wire unchanged records nothing (the stable request clears the latch — existing latch semantics need no new code); in-memory run-scoped like the other monitor state | `packages/coding-agent/test/suite/prefix-monitor.test.ts` (settings-change describe: image-toggle attribution + no-op toggle records nothing) |
| `packages/ai/src/types.ts` | Optional `onWireRewrite?: (cause: string) => void` observer on `StreamOptions` (issue #56 seam) | none (type-only) | Additive optional field: every adapter and SDK consumer compiles and behaves unchanged; adapters that report nothing never invoke it, and the callback must never affect the request | `packages/ai/test/anthropic-wire-rewrite.test.ts` |
| `packages/ai/src/api/simple-options.ts` | `buildBaseOptions` forwards `onWireRewrite` (one field copy) | none | Without it the `streamSimple` → `stream` option rebuild drops the callback, and the coding-agent monitor (which injects it into the options it forwards) would never observe a rewrite | `packages/ai/test/anthropic-wire-rewrite.test.ts` (all cases go through `streamSimple`) |
| `packages/ai/src/api/anthropic-messages.ts` | Two `onWireRewrite` fire points (issue #56): the auth-mode transition at the OAuth shaping branch in `buildParams` and the deferred-load anchor at the `loadedToolNames` transition in `convertToolResult` (callback threaded through `convertMessages`); both track their previous-value state in a `WeakMap` keyed on the observer callback, so each consumer's callback tracks its own transitions | none (fork-only diagnostic reporting) | Stateless request behavior is unchanged — the tracked state only gates whether the optional callback fires, and no-observer requests are byte-identical (they track nothing); the coding-agent monitor injects one stable bound method per session and `StreamOptions.onWireRewrite` documents the stable-callback contract, so attribution is per-session and the tracking dies with the observer; fires happen during serialization, before the HTTP call, so HTTP retries do not double-fire; since the 2026-09-03 sync the fire sites are unchanged (`buildParams` / `convertToolResult`) — upstream's restructure only changed the plumbing around them (`ConvertedAnthropicMessages` return, `managedProvider` param), and the callback now threads through `convertMessages` to reach `convertToolResult` | `packages/ai/test/anthropic-wire-rewrite.test.ts` (including interleaved-session isolation), `packages/ai/test/deferred-tools.test.ts`, `packages/ai/test/anthropic-auth-token.test.ts` |
| `packages/coding-agent/src/core/sessions/prefix-stability.ts` | Two `PrefixInvalidationCause` members (`provider-deferred-tool-load`, `provider-auth-mode`) excluded from `PrefixInvalidationExpectation`, plus the module-private `PROVIDER_WIRE_REWRITE_CAUSES` list and the exported `isProviderWireRewriteCause` narrow (issue #56) | none (vocabulary module) | The provider causes are never latch-announced (they count directly at report time), so excluding them from the expectation union prevents misuse; unknown future cause tags stay uncounted instead of corrupting the stats | `packages/coding-agent/test/prefix-stability.test.ts`, `packages/coding-agent/test/suite/prefix-monitor.test.ts` (wire-rewrite describe) |
| `packages/coding-agent/src/core/agent-session.ts` | Wire-rewrite consumer (issue #56): moved to `core/sessions/request-observer.ts` with the streamFn observer (issue #60) — the wrapper injects `onWireRewrite` into the options it forwards and counts known causes directly, once per cause per request, never on the first observed request, without touching the expectation latch; nothing observer-side remains in agent-session.ts | `core/sessions/request-observer.ts`, `core/sessions/prefix-stability.ts` | Counting is diagnostic-only and swallowed on error; provider causes emit no `prefix_invalidated` event (announced-cause semantics); the injected options spread adds one small object per request on a path that already spreads options | `packages/coding-agent/test/suite/prefix-monitor.test.ts` (wire-rewrite describe: dynamic-tool anchor attribution, auth-mode attribution, first-request initialization, unknown-tag ignore, no latch leak into a later unannounced divergence) |
| `packages/coding-agent/test/suite/harness.ts` | Optional `streamFn` harness option (default: the faux provider's `streamSimple`) | none (test utility) | The AgentSession monitor wrapper sits above the agent's streamFn, so a custom streamFn receives the monitor-injected options — used to simulate adapter wire-rewrite reports at the true fire point (inside the stream call); existing harness callers unchanged | `packages/coding-agent/test/suite/prefix-monitor.test.ts` (wire-rewrite describe) |
| `packages/coding-agent/src/core/sessions/request-observer.ts` | Fork-owned module (issue #60): `ProviderRequestObserver` — the provider-request observer cluster (transparent streamFn wrapper with the `unwrapped` marker plus exported `unwrapStreamFn`, prefix serialize+diff with blockImages announce-before-diff, request-kind claiming and usage recording, wire-rewrite cause counting, retry markers and summarizer kind windows) extracted from `AgentSession` with injected dependencies and public renames | none (fork-owned file; no upstream counterpart) | Behavior-identical move: deps are injected (`prefix_invalidated` emitter typed as an `Extract` of the unchanged `AgentSessionEvent` union — type-only import, no runtime cycle — and a `getBlockImages` getter), so the module knows neither the event bus nor the settings manager; agent-session.ts keeps the wiring (constructor install, announce/mark/window one-liners, `getSessionStats` accessors, `unwrapStreamFn` import); tests pass unchanged | `packages/coding-agent/test/suite/prefix-monitor.test.ts`, `packages/coding-agent/test/suite/cache-stats.test.ts`, `packages/coding-agent/test/prefix-stability.test.ts`, `packages/coding-agent/test/agent-session-stats.test.ts`, `packages/coding-agent/test/suite/agent-session-compaction.test.ts` |
| `packages/coding-agent/src/core/extensions/types.ts`, `packages/coding-agent/src/core/agent-session.ts` (`getAllTools`) | `ToolInfo` gains `capability` (one field added to the Pick plus one line at the construction site) so extensions can match tools by capability exactly like the core permission policies do (issue #69) | none (extension-API enrichment; pairs with `examples/extensions/read-only-mode.ts`) | Additive optional field: existing `ToolInfo` consumers compile and behave unchanged; the value is the same `ToolDefinition.capability` the policy engine reads, so extension-side matching and core-side enforcement cannot drift on builtins | `packages/coding-agent/test/read-only-mode-example.test.ts` (capability-based hiding and blocking) |
| `packages/coding-agent/src/index.ts` | Re-export the permission engine's public surface (`evaluatePermission`, `evaluatePermissionLayered`, `resolveProfileConfig`, `TOOL_CAPABILITIES`, and the rule/decision/profile types) from the package root so extensions can import it (issue #90) | `core/tools/index.ts` (already re-exports all of it) | Export-list addition only, no runtime change; the engine itself is untouched — `examples/extensions/read-only-mode.ts` and any third-party permission extension import the identical exported evaluator (the built-in `src/extensions/permission-policies/index.ts (folder with README)` imports the core module directly), so semantics cannot drift between the builtin, the examples, and third-party extensions | `packages/coding-agent/test/permission-policies-builtin.test.ts`, `packages/coding-agent/test/read-only-mode-example.test.ts` |
| `packages/coding-agent/vitest.config.ts` | Self-alias `@earendil-works/pi-coding-agent` to `./src/index.ts` | none (test-configuration only) | Examples and some tests import the package by its own name; without the alias they resolved the stale `dist/` build and silently exercised old code — first visible when the evaluator's return shape changed under them. Same alias pattern the config already applies to every sibling workspace package | The example test suites (`test/read-only-mode-example.test.ts` resolves the example's package-name import through the alias; `test/permission-policies-builtin.test.ts` imports relative paths and does not) |
| `packages/coding-agent/src/extensions/index.ts` | Two `builtInExtensions` entries: the config-gated permission-policies builtin and the config-gated model-handoff builtin (plus their imports and comments) | `src/extensions/permission-policies/index.ts (folder with README)`, `src/extensions/model-handoff/index.ts (folder with README)` | Upstream-owned file: the entire change is the array literal plus imports, so an upstream merge conflicts at most there and the entries re-add mechanically; both factories no-op without their config files, so sessions without them behave exactly as upstream | `packages/coding-agent/test/permission-policies-builtin.test.ts` (registration, gating, trust), `packages/coding-agent/test/suite/model-handoff.test.ts` (activation, handoff, guards, return, project config) |
| `packages/ai/src/models.ts` | `getSupportedThinkingLevels` offers `xhigh` for reasoning models with no `thinkingLevelMap` (earendil-works/pi#4344): uncurated vocabulary, the wire arbitrates. One change point feeds the cycle, the settings picker, the server protocol report, and the OpenAI-style adapters' send-time `clampThinkingLevel` (built on this function) — which is why a cycle-side-only change cannot implement this for those APIs: the clamp would drop xhigh before the wire | none (library-level rule) | Deliberate error-driven divergence: the OpenAI-style adapters clamp then send `thinkingLevelMap?.[level] ?? level`, so an unsupported xhigh reaches the provider raw and fails the request with an actionable error (consequence: `clampThinkingLevel` on a mapless model now lands `max` on `xhigh` instead of `high`, so a persisted `max` default on a mapless model sends raw xhigh); the Anthropic and Bedrock adapters map unmapped levels straight to effort `high` without clamping (silent fallback, accepted); Google's `resolveGoogleThinkingLevel` degraded unmapped `xhigh`/`max` to effort `high` since issue #124 (the original client-side rejection shipped with this row and reversed there: the Google resolver cannot send an unmapped level, so the "actionable error" fired on every request once a persisted xhigh default crossed onto a mapless Google model — release-smoke evidence on #124); zai models with `supportsReasoningEffort: false` (glm-4.7) send only `thinking: enabled`, so xhigh is a UI label there. A map — including the 118 catalog maps that omit the `xhigh` key for known-unsupported models — stays authoritative, `max` stays opt-in, and the 495 mapless catalog models gain one cycle stop. 2026-09-03 (#139/#140): the generator pins `xhigh: null` on the seven mapless Azure o-series/realtime mirrors (exact-id set — prefix matching would over-match future o1k/o10 names) so the offer rule cannot advertise a level that API rejects with a 400; the data JSON is gitignored and regenerated by CI/releases, so the tracked rule plus a hydrated-catalog test is the complete fix | `packages/ai/test/max-thinking.test.ts` (mapless xhigh offered + clamp; map-omitting-xhigh stays hidden), `packages/ai/test/supports-xhigh.test.ts` (Sonnet 4.5 uncurated case) |
| `packages/ai/scripts/generate-models.ts` | Curation rule pinning `xhigh: null` on the seven mapless Azure o-series/realtime mirrors via an exact-id set (issue #139/#140) | `packages/ai/src/models.ts` row (the offer rule the pin guards) | The data JSON under `src/providers/data/` is gitignored and regenerated by builds/CI, so the tracked rule plus a hydrated-catalog regression test is the complete fix; exact ids because prefix matching would over-match future `o1k`/`o10`-style names; mirrors the file's existing curation patterns (gpt-5 `off: null`, Together/Qwen maps) | `packages/ai/test/supports-xhigh.test.ts` (pinned Azure mirrors) |
| `packages/ai/src/api/google-shared.ts` | `resolveGoogleThinkingLevel` degrades unmapped `xhigh`/`max` to `high` instead of throwing (issue #124, release blocker) | `packages/ai/src/api/models.ts` row above (the xhigh offer rule that made unmapped levels reachable) | Parity with the Anthropic, Bedrock, and Mistral adapters' unmapped-level fallback: the Google resolver cannot send an unmapped level, so throwing meant a persisted xhigh default crossing onto a mapless Google model failed every request client-side (observed live on `--model google/gemini-2.5-flash` and after a model-handoff switch). Explicit garbage and `null` mappings still throw; mapped values stay authoritative. Display divergence accepted and recorded: the footer shows the requested level while the wire sends `high`, the same wire-only divergence zai's label-only xhigh already carries | `packages/ai/test/google-thinking-level-map.test.ts` (degradation, garbage throw, gen-ai and Vertex wire payloads with the degraded budget) |
| `packages/ai/README.md` | Documents the `StreamOptions.onWireRewrite` observer for SDK consumers (what the adapter reports, the stable-callback contract, diagnostic-only guarantee) in the README section extension and SDK authors read about streaming options (issue #56; companion to the seam rows above) | none (documentation) | Docs-only; on sync, keep the section while the seam survives and drop it with the seam if the seam is ever removed; the code example matches the `anthropic-wire-rewrite` test's usage | The doc build/lint (`npm run check`) |
| `packages/coding-agent/examples/extensions/gondolin/index.ts` | The gondolin example re-registers the built-in grep with a VM-backed execute, so the counts-first grep renderer (the `core/tools/grep.ts` row) needs the true match count stamped here too: `executeGondolinGrep` sets `details.matchCount` and now always returns `details` (empty-object check dropped, since the count is the point); without it the renderer's notices-stripped fallback counts context lines and notices as matches in gondolin sessions (shipped with the #102 renderer work, unledgered until the #145 audit) | none (example-local companion to the grep renderer row) | Only the match-count stamp and the unconditional `details` return change; the VM execute logic is upstream's | `packages/coding-agent/test/tool-previews.test.ts` pins the built-in path; the gondolin delta is compile-checked by `npm run check` |
| `scripts/profile-coding-agent-node.mjs` | Telemetry purge (issue #32) follow-through: `PI_SKIP_VERSION_CHECK` removed from the benchmark env and the `--no-offline` help line — no code reads the env var anymore | none (fork-local policy) | Script-only; benchmark behavior otherwise unchanged (`PI_OFFLINE=1` still forced by default) | The script's `--help` output |
| `packages/coding-agent/examples/extensions/custom-compaction.ts`, `packages/coding-agent/docs/compaction.md` | The example taught the cache-hostile summarizer shape the fork's core compaction replaced (serialized `<conversation>` blob, `cacheRetention: "none"`, minted `sessionId`, and a different model — provider caches are per model, so that alone forfeits the replay's point); it now replays the prefix through public API only — session model (`ctx.model`), `ctx.getSystemPrompt()`, active tools ordered by `pi.getActiveTools()`, `convertToLlm(replayMessages + turnPrefixMessages)`, one appended instruction user turn, `ctx.sessionManager.getSessionId()` as the routing id, no cache opt-out. `compaction.md`'s "Message Serialization" section described the old standalone default as current and its custom-summary section taught serialization first; both now state the replay default, the split-turn serialization exception, and the cache trade of a different-model summarizer (issue #146) | none (example/docs; core compaction stays the byte-exact default) | The replay rides `CompactionPreparation.replayMessages`, which was already extension-visible; byte-exactness caveats live in the example header (`getSystemPrompt()` excludes chained/payload rewrites, `ToolInfo` lacks constrained sampling) and are why the core default is not replaceable byte-for-byte by an extension — an extension compaction is also invisible to the prefix monitor (`before_provider_request` never fires for it; see the placement re-audit row). On sync conflict in the example, take upstream's shape and re-apply the replay pattern | `packages/coding-agent/test/custom-compaction-example.test.ts` (prefix replay, instruction folding of `previousSummary`, routing id, empty-summary and error fallbacks); `test/compaction-extensions-example.test.ts` keeps the docs-snippet compile checks (its old dispatch test is superseded by the new file) |
| `packages/coding-agent/src/core/tools/task.ts`, `packages/coding-agent/src/core/tools/renderers/task.ts` | Fork-owned modules (sub-agent `task` tool; see the placement decision row): the tool definition (LLM-provided `prompt` + optional `systemPrompt`, per-call nested `Agent` with the active toolset minus `task`, model/thinking inherited at spawn time, abort wired from the tool signal, a per-spawn wall-clock deadline read live from the `taskTimeoutMs` setting (2026-09-20; default 600000 ms, 0 disables — agent-loop discipline: unref'd timer cleared on settle, `AbortSignal.any` with the run signal, a race because a hung provider stream never settles on its own, ending in `Sub-agent timed out after Nms` instead of a held limiter slot), final assistant text + summed usage as the result), the session-owned `SubAgentLimiter` (FIFO slot handoff — release hands the slot to the next waiter without decrementing, so no microtask gap lets a concurrent acquire double-book), and the TUI renderers (prompt preview on the call line; each sub-agent renders as a `Task N` preview window — the shared `preview-window.ts` machinery the hidden-thinking preview uses: live header with breathing dots and a running timer over a fixed-height faded tail of the activity line and streamed text, then a frozen-duration header `Task N · 4.2s · M tool calls` (duration persisted as `details.durationMs` so resumed rows keep it; the live header owns a 1s re-render interval in renderer state — cleared on the final result and by the component's destroy() — because sub-agent tool calls run silent past the generic row ticker) with a natural-height tail window collapsed and the sub-agent's whole conversation expanded — prompt, thinking, assistant turns, every tool call with its result as a head+tail excerpt; an `N sub-agent tokens` line from the result usage; session-scoped `Task N` numbering continues past resumed history via a session counter — assigned at spawn-acquisition order, so failed runs leave gaps and a queued earlier call can number after a later one) | none (fork-owned files; no upstream counterpart) | The tool takes session state through injected getters (`TaskToolOptions`) so `_buildRuntime` closures read live values; sub-agents are ephemeral (no session persistence, no compaction — fresh tiny contexts) and stream through `unwrapStreamFn(this.agent.streamFunction)`; errors throw and become error tool results — which also means a failed run carries no transcript (the runtime's error-result path has no details channel; reopening that needs an upstream error-details seam, not a fork workaround that returns failures as successes); the activity, task number, and transcript ride the renderer-only `details` payload (never the model-visible content; `executionStartedAt` added additively to `ToolRenderContext` for the live timer) — the transcript excerpts each message to 8 head + 8 tail lines with each line truncated at 2000 chars via `truncateLine` (2026-09-20; the line-count bound alone let one minified 200 KB single-line result survive), previews call arguments via preferred keys (`command`/`path`/`pattern`/… — the model's JSON key order is arbitrary) then the first string value, holds the last 500 entries with `dropped` counting older ones and `totalCalls` the true call count, is attached only when the sub-agent made at least one tool call (a tool-free run's transcript would just repeat the call line and the result body), and persists with the tool result message (bounded, comparable to one capped bash output); the renderer's head/expand block intentionally matches the ls/find idiom rather than extracting a helper (per-tool copies differ in limits/highlighting — see renderers/ls.ts) | `packages/coding-agent/test/suite/task-tool.test.ts` (instructions/systemPrompt reach the sub-agent, `task` absent from its toolset, real tool execution inside the sub-agent, live activity partials + the returned transcript with real result content, the `tool_call` extension gate blocking a sub-agent call, the concurrency cap via gated faux responses, abort, limiter FIFO invariants); `packages/coding-agent/test/tool-previews.test.ts` (renderer: activity line, collapsed summary, expanded conversation, dropped-messages marker, token line) |
| `packages/coding-agent/src/core/tools/index.ts`, `packages/coding-agent/src/core/tools/renderers/index.ts` | `task` joins `ToolName`, `allToolNames`, the `createToolDefinition`/`createTool` switches, `createAllToolDefinitions`/`createAllTools`, and the renderer record; `requireTaskToolOptions` throws at construction when the session-provided getters are missing, so an SDK caller assembling built-ins without a session gets an explicit error rather than a tool that fails at execute time | `core/tools/task.ts` | Additive union member: every `Record<ToolName, ...>` consumer (including `createAllToolRenderers`) is forced complete by the type; `createCodingTools`/`createReadOnlyTools` deliberately exclude task (curated subsets) | `packages/coding-agent/test/default-tools-setting.test.ts`, `test/builtin-tool-strict-mode.test.ts`, `test/tool-previews.test.ts` (updated for the ninth built-in; `stubTaskToolOptions` in `test/utilities.ts` serves the characterization suites) |
| `packages/coding-agent/src/core/agent-session.ts` | Sub-agent wiring: the `_installAgentToolHooks` closures become the shared `_beforeToolCallHook`/`_afterToolCallHook` fields (assigned to the main agent and passed to each sub-agent, so permission policies, `tool_result` interception, image normalization, and output bounding apply to sub-agent tool calls unchanged); `_buildRuntime` passes the task tool its live-state getters plus the `unwrapStreamFn` stream function; a session-owned `_subAgentLimiter` field survives registry rebuilds; `task` joins the default active list | `core/tools/task.ts`, `core/sessions/request-observer.ts` (`unwrapStreamFn`) | Hook bodies are byte-identical to the closures they replaced (they read `this._extensionRunner` at execution time, so reload semantics are unchanged); the sub-agent filter is `agent.state.tools` minus `task` read at spawn time, so mid-run tool-set changes affect only later spawns | `packages/coding-agent/test/suite/task-tool.test.ts` |
| `packages/coding-agent/src/core/sdk.ts` | `task` joins `defaultActiveToolNames` | `core/tools/task.ts` | Mirror of the agent-session default list; sessions with a configured `defaultTools` array keep replacing the default list, so such installs opt in by adding `"task"` | `packages/coding-agent/test/default-tools-setting.test.ts` |
| `packages/coding-agent/src/core/settings-manager.ts` | New `maxSubAgents` setting (integer `>= 1`, default 2, no upper cap; `getMaxSubAgents()` throws on invalid values like the compaction token getters) | `core/tools/task.ts` | Additive optional flat setting; read at acquire time so mid-session changes take effect; excess spawns queue, never error | `packages/coding-agent/test/settings-manager.test.ts` |
| `packages/coding-agent/src/modes/interactive/components/tool-execution.ts` | `updateResult` and the stored result carry an optional `usage` field, passed through to `renderResult` (the task renderer's token line reads it; `ToolResultMessage.usage` documents the field, but the component previously dropped it) | `core/tools/renderers/task.ts` | Additive optional field; renderers that ignore it are unchanged, and the interactive-mode call site already spreads `event.result` so no wiring change was needed there | `packages/coding-agent/test/tool-previews.test.ts` (token line), `test/tool-execution-component.test.ts` (unchanged behavior) |
| `.pi/extensions/tps.ts` | Upstream's dogfooded TPS notify measured `agent_start`→`agent_end` wall time, so a run executing tools divided its output tokens by tool-execution and queue time too (a ~20 tok/s turn read as 3.3 after a 25s tool call); the fork version sums per-request streaming windows (`message_start`→`message_end`, assistant messages only, unpaired aborted windows dropped) and divides by that, and trims the line (drops the redundant `total` — it is in+out+cache — hides `cache r/w` at 0/0, k-notation, sub-second streams get two decimals); 2026-09-06 (footer-overlap cleanup): the line is turn-performance only — the run's `↑` input and `cache %`/`w` cache-write fields are dropped because the footer already shows session token totals and Cache % (same hit-rate formula) two lines away and the `↑`/`↓` glyphs meant different scopes in each place; the line reads `tok/s ↓out (N thinking) duration` and the input/cache accumulators went with the fields; 2026-09-06 (same round, part two): the line left the transcript entirely — it now flashes on the footer's pwd line via the new `ctx.ui.transientStatus` instead of `ctx.ui.notify`, whose chat status row persisted in the scrollback once the next prompt followed | none (project-local dogfooding extension; not shipped in any package) | On sync conflict take upstream's shape and re-apply the streaming-window timing, the trimmed turn-performance line, and the `ctx.ui.transientStatus` delivery; the stats land on `agent_end` as a transient footer message | Manual: a tool-call turn in tmux reports the post-tool request's speed, not wall time |
| `packages/coding-agent/docs/extensions.md`, `docs/sessions.md`, `docs/usage.md`, `docs/packages.md`, `examples/extensions/README.md`, `core/compaction/utils.ts`, `core/model-resolver.ts`, `AGENTS.md`, `core/export-html/tool-renderer.ts` | Companion edits that had no row of their own, folded here so every upstream-owned touch greps (#145 audit): extensions.md documents `ToolDefinition.timeoutMs` (extensions/types.ts row); sessions.md documents the /session Tools volume line and Cache block (the interactive-mode /session rows); usage.md's `pi update --models` line says local restore (remote-catalog-provider row); packages.md drops the managed-install sentence (the not-ported decision row); the examples README gains the read-only-mode table row (read-only-mode example); compaction/utils.ts adds `SUMMARIZER_PERSONA` carried by the replay instruction (compaction replay rows); model-resolver.ts moves the Cloudflare gateway default (Catalog-drift hardening section); AGENTS.md carries the fork's git/release/fetch-only rules (upstream fetch-only decision row); 2026-09-12 (task tool): settings.md's Tools section gains the `maxSubAgents` row and the task paragraph, quickstart.md's default tool list gains `task`, and sdk.md's Tools section lists `task` among built-in names and defaults with the `TaskToolOptions` construction note, while `core/export-html/tool-renderer.ts` passes `executionStartedAt: undefined` because the export renderer has no live start time (companion to the task tool's additive `ToolRenderContext.executionStartedAt` field in extensions/types.ts, named in the task module row) (the task tool rows); 2026-09-20 (placement-hardening round): usage.md's Design Principles state the fork's one divergence (the built-in task tool), and settings.md's Tools table gains the `taskTimeoutMs` row with its setTimeout-ceiling bound (the placement-hardening round row) | none (documentation and companion edits) | Each edit documents or accompanies an existing feature row; the brace-listed rows (`packages/ai/src/api/{...}` telemetry purge, the builtin tools row) cover their member files the same way this row covers these | The owning rows' tests |

| `packages/coding-agent/src/core/skills.ts`, `packages/coding-agent/src/core/agent-session.ts`, `packages/coding-agent/src/index.ts` | Skills-anywhere expansion (0.85.5; recorded 2026-09-13): `expandSkillCommands` matches `/skill:name` tokens anywhere in the text (`(^|\s)` boundary, word/hyphen name charset so trailing punctuation like `/skill:tdd,` still resolves), replaces each in place keeping the surrounding text in order, passes unknown names and non-boundary tokens (`path/skill:x`) through as literals, and reports skill-file read failures through `onError`; agent-session's `_expandSkillCommand` becomes the wrapper, still called at upstream's two expansion sites (the prompt path gated on `expandPromptTemplates`, and `_queueUserInput`'s steer/followUp path) with failures emitted through the extension error channel (`skill_expansion`); `parseSkillSegments`/`SkillMessageSegment` join the kept-and-still-exported `parseSkillBlock` as the multi-block parser; index.ts exports the three new symbols | `core/skills.ts` (the expansion itself) | Upstream's leading-token matcher appended args as `User: <args>`; the fork's in-place replacement makes the args the surrounding text (documented in skills.md); expansion still runs after the `input` extension event; a message whose tokens all fail to resolve is stored and sent verbatim; the `<skill name location>` block format is unchanged, so sessions written before and after render alike | `packages/coding-agent/test/skill-expansion.test.ts` |
| `packages/coding-agent/src/modes/interactive/interactive-mode.ts` (user-message rendering and command registration only), `packages/coding-agent/src/core/export-html/template.js` | Skills-anywhere rendering: the user-message case iterates `parseSkillSegments` and alternates `SkillInvocationMessageComponent` and `UserMessageComponent` siblings in message order (upstream rendered one leading block via `parseSkillBlock`); template.js's single-block `parseSkillBlock` becomes segment-based with the same interleaved layout; interactive-mode also flags its skill commands `midTextInvocable: true` when registering them with the editor's autocomplete (the pi-tui row below) | `core/agent-session.ts` (`parseSkillSegments`) | Reuses upstream's existing skill-invocation component and the export's `skill-invocation` entry markup; no stored-format change; this file's other fork deltas carry their own rows | `packages/coding-agent/test/export-html-skill-block.test.ts` |
| `packages/tui/src/autocomplete.ts`, `packages/tui/src/components/editor.ts` | Mid-text slash completion (tui 0.85.5; recorded 2026-09-13): `SlashCommand.midTextInvocable` opts a command into mid-text invocation; `activeSlashToken` extracts the `/`-token being typed (line start with no arguments yet, or the token after whitespace on any line); the provider offers mid-text-invocable commands for such tokens and falls through to path completion on no match; the editor auto-triggers autocomplete when `/` is typed at a whitespace boundary (or at the start of the message) instead of only at the first line's start, and Tab still routes only first-line command names (no arguments yet) to slash completion | none (library-additive: `midTextInvocable` unset keeps every existing command and consumer unchanged) | First-line leading `/` keeps the full command list and argument completions; mid-text tokens offer invocable commands only; the fork's coding-agent is the shipping consumer | `packages/tui/test/autocomplete.test.ts`, `packages/tui/test/editor.test.ts` |
| `packages/coding-agent/docs/skills.md` | Documents mid-text and chained `/skill:name` invocation, the whitespace-boundary and unknown-name rules, and the anywhere-`/` autocomplete (skills-anywhere companion) | none (documentation) | Docs-only; on sync conflict take upstream's shape and re-apply the fork section | The doc build/lint (`npm run check`) |
| `packages/coding-agent/src/core/extensions/runner.ts`, `packages/coding-agent/src/modes/rpc/rpc-mode.ts` | `ctx.ui.transientStatus` no-op wiring for the non-TUI modes (companion to the transientStatus decision row): the runner's shared no-op UI context (print/json modes, no footer) gains the method, and RPC mode implements it as a documented no-op like its neighboring `setWorkingMessage` | `modes/interactive/components/footer.ts` (the real implementation) | No-op by design: the transient slot is footer chrome those modes do not have; extension callers degrade silently | Covered by the existing footer transient-status tests (no new surface) |
| `scripts/local-release.mjs` | Bun consumer smoke test resolves bun's real binary (`bun -e 'process.stdout.write(process.execPath)'`) and hands it to the smoke runner instead of invoking the `bun` shim: the consumer smoke runs under a sanitized env with an isolated HOME, which breaks version-manager shims (mise resolves its config from HOME and reports "not a valid shim") | none (script-local) | Only the local release smoke path changes; a failed probe is a hard error; the npm-install smoke is unchanged | The `npm run release:local` run itself |

### Note on `PackageManager.checkForAvailableUpdates` (issue #29 review)

The telemetry purge removed its only production caller (the automatic startup extension update check), leaving the public method and its private helpers test-only. Deleting them was attempted and reverted: removal cascades through upstream-owned private helpers and their upstream tests, costing more merge divergence than the dead code costs. The method stays as-is; `pi update --extensions` still uses the package manager's update path. Revisit only when rebuilding the extension-update UX.

## Outbound Traffic Audit (issue #32)

Every outbound endpoint found by grepping runtime source (`packages/*/src`) for `fetch`/`http`/URL literals, with payload, trigger, and disposition. Policy: user-configured provider APIs (provider-required headers only) + explicit user-initiated actions; everything else purged. 2026-09-05 amendment: two behaviors returned as strictly opt-in settings (default off, asked once by the first-run wizard) — see the `api.github.com` and OpenRouter attribution rows.

| Endpoint | Payload | Trigger | Disposition |
| --- | --- | --- | --- |
| `pi.dev/api/report-install?version=X` (GET) | pi version in query; UA `pi/<version> (<platform>; node/<v>; <arch>)` | Interactive init on fresh install and on changelog-detected version bump | Removed: `reportInstallTelemetry` deleted along with the `enableInstallTelemetry` setting (default ON), `PI_TELEMETRY` env, `/settings` toggle, and `core/telemetry.ts` gate |
| `pi.dev/api/latest-version` (GET) | pi version via UA | Interactive startup `checkForNewPiVersion` (update banner) and `pi update --self` plan | Removed: `utils/version-check.ts` deleted; startup check + `showNewVersionNotification` banner deleted; self-update installs `<pkg>@latest` via npm with no version lookup |
| `api.github.com/repos/R-Dson/pi/releases/latest` (GET, unauthenticated) | none — reads `tag_name` only | Interactive startup, only when the `updateCheck` setting is enabled (default off; asked once by the first-run wizard, toggleable in `/settings`); skipped under PI_OFFLINE; silent on any failure | Kept: opt-in (2026-09-05) — the fork's only automatic non-provider request; replaces the deleted pi.dev version check with one against the fork's own releases |
| `pi.dev/api/models/providers/<id>` (GET) | provider id via path; pi UA; etag revalidation | Startup/model-selector catalog refresh for every builtin provider except radius; `pi update --models` | Neutralized: `withRemoteCatalog` restores only the persisted local models-store overlay; network revalidation, etag/304/TTL logic, and `catalogBaseUrl` removed |
| OpenRouter requests: `HTTP-Referer: https://pi.dev`, `X-OpenRouter-Title: pi`, `X-OpenRouter-Categories: cli-agent` | app identification | Every OpenRouter request (incl. custom providers routed via openrouter.ai) | Removed 2026-08-23 (issue #32); restored 2026-09-05 as opt-in: the `providerAttribution` setting (default off) re-adds the headers with fork values (`HTTP-Referer: https://github.com/R-Dson/pi`, `X-OpenRouter-Title: pi-fork`) on OpenRouter requests only; non-OpenRouter providers never receive identifying headers |
| NVIDIA NIM requests: `X-BILLING-INVOKE-ORIGIN: Pi` | app identification | Every nvidia / integrate.api.nvidia.com request | Removed: undocumented billing-attribution header; only `Authorization` is required |
| Cloudflare requests: `User-Agent: pi-coding-agent` | app identification | Every cloudflare-workers-ai / cloudflare-ai-gateway request | Removed; the pi app User-Agent is also removed from all packages/ai provider APIs (node's default UA only; GitHub Copilot keeps its required client UA) |
| OpenCode requests: `x-opencode-session: <sessionId>`, `x-opencode-client: pi` | session id + client name | Every opencode / opencode-go / opencode.ai request | Kept (provider-required): the Zen gateway serves anonymous header-less requests from a heavily restricted rate-limit tier, making the CLI effectively unusable without them (upstream earendil-works/pi#2824); minimum kept, documented in `core/provider-attribution.ts` |
| `settings.json`: `trackingId` (randomUUID) + `enableAnalytics` opt-in | local tracking identifier; no transmitter existed in-tree | First-time-setup analytics step; `setEnableAnalytics` | Removed: analytics step and settings fields deleted; 2026-09-05 the wizard gained default-off `updateCheck`/`providerAttribution` questions (a different, still privacy-first ask) |
| `registry.npmjs.org` (`npm view`) / `git ls-remote` per installed extension package | installed package names | Automatic startup extension-update check (`checkForPackageUpdates`) | Removed: automatic check + `showPackageUpdateNotification` deleted; `pi update --extensions` performs the same checks only when run explicitly |
| Provider endpoints (chat streaming, auth/availability probes, OAuth login/refresh) | user credentials + provider-required auth and headers only (no app User-Agent) | User-configured providers | Kept: category (a) — this is the product |
| `api.github.com/repos/{sharkdp/fd,BurntSushi/ripgrep}/releases/latest` + release assets | UA `pi-coding-agent`; tool/platform via asset name | `ensureTool` when fd/rg are missing from both the managed tools dir and PATH (first run) | Kept: one-time functional dependency download (GitHub only, no install-identifying payload, silent after first download); removing it would break the grep/find tools |
| `huggingface.co/api/models` (+ optional HF token) and the local llama.cpp server | model search queries | `/llama` command (model picker, download approval) | Kept: explicit user command |
| `gh gist create` (api.github.com), `radius.pi.dev/v1/artifacts` | exported session HTML/JSONL | `/share` command | Kept: explicit user command; `pi.dev/session/` remains a display-only viewer link prefix (no fetch) |
| `pi.dev/changelog` (text), `pi.dev/session/<id>` (text) | none — display strings | Former update banner (removed); share viewer link | Display-only; no fetch either way |
| `radius.pi.dev/v1/bug-reports` (POST, multipart) | the redacted report bundle (same content as the zip export) | `/bug` → "Upload Report" delivery choice, behind an explicit consent dialog that names what is sent; anonymous by default, optionally attributed to the user's Radius account token | Kept (arrived with the 2026-09-21 sync): explicit user-initiated action, never automatic; "Export as Zip" keeps a fully local path |
| `radius.pi.dev` default gateway | provider traffic with user OAuth token | radius provider (user-configured) | Kept: a provider gateway that happens to live on the pi.dev domain, not telemetry |
| `packages/telemetry` (noop/memory span contracts) | none — in-process spans | `pi` runtime wires no remote sink | Kept: contracts/adapters only; nothing transmits |

### Catalog-drift hardening (issue #40 CI fix)

The Cloudflare live catalog dropped its last openai-completions gateway models between CI runs. Two consequences, both fixed on this branch:

- `cloudflare-ai-gateway.ts` pins the `createProvider` generic instead of letting it infer from the generated catalog; an empty API subset no longer breaks the static api entry. Upstream adopted the same pin as the named `CloudflareAIGatewayApi` type (e8c632ef6, 2026-08-25 sync); the fork keeps only the explanatory comment above the type.
- Tests pinning the gateway kimi model (`getModel("cloudflare-ai-gateway", "workers-ai/@cf/moonshotai/kimi-k2.6")`) are guarded with `it.skipIf(!getModel(...))` so they run exactly when the catalog offers the model again; the two `model-runtime-cloudflare-compat.test.ts` tests guard on `getBuiltinModels` the same way. The full guarded set (recorded 2026-09-13 so the diff greps): `cross-provider-handoff.test.ts`, `empty.test.ts`, `openai-completions-empty-tools.test.ts`, `stream.test.ts`, `tokens.test.ts`, `tool-call-without-result.test.ts`, `total-tokens.test.ts`, `unicode-surrogate.test.ts`.
- The gateway's default model in `model-resolver.ts` moved to `claude-sonnet-4.5` (present in the current catalog); the resolver test iterates all defaults against the generated catalogs, so a vanished default breaks the build the same way.

## Phase 8 Compatibility Audit (issue #13)

Plan phase 8 says: after at least one stable release, remove temporary compatibility paths. Audit of everything the fork added, and each item's disposition:

| Path / flag | Disposition |
| --- | --- |
| Dual projection execution | Never existed — the projector extraction replaced the inline code and the golden tests proved equivalence. Nothing to remove. |
| `tools.permissions.mode` (legacy \| policy, default legacy) | Removed 2026-08-29 (issue #70): enforcement ships only as the permission-policies extension; settings keys are ignored. |
| `tools.maxToolOutputBytes` | Keep. Configuration, not a temporary flag. |
| Session readers (v1/v2/v3 migrations) | Keep — upstream-owned, retained indefinitely per the plan. |
| Legacy tool adapters | None were added (`AgentTool` gained only optional `timeoutMs`; `ToolDefinition` only optional `capability`). |
| Golden/equivalence tests | Keep — permanent regression baselines, not scaffolding. |
| `recoverSessionEntries`, `--validate-session` | Keep — the recovery read API and its diagnostic surface. `recoverSessionEntries` deliberately has no startup wiring: upstream's tolerant loader already drops the torn tail line and the validator reports it, so repair-at-load has no remaining job (interrupted-turn repair is the separate resume path). |

Removal set today: the core `tools.permissions` policy mode (removed 2026-08-29, issue #70; breaking change recorded in the coding-agent changelog).

## Sync log

| Date | Upstream range | Conflicts | Notes |
| --- | --- | --- | --- |
| 2026-09-22 | 4c8eb393c..1a584a7a5 (2 commits: Grok 4.7 support — xAI long-context pricing metadata via a shared `getModelsDevCost` in generate-models, default xAI model switched to grok-4.7, catalog/test updates; empty text parts omitted from multimodal user messages #9797, so image-only prompts are no longer rejected by strict OpenAI-compatible providers) | 2: ai + coding-agent CHANGELOGs (union — upstream's [Unreleased] entries folded under the fork's: Grok 4.7 under ai Added plus a new Fixed section, the default-xAI-model entry under coding-agent Changed) | Small additive sync, both commits adopted wholesale. `generate-models` re-run (standing hazard from the 2026-09-21 sync): grok-4.7 and its pricing tiers ride in the git-ignored `providers/data` snapshots, so no tracked catalog churn (`models.generated.ts` unchanged). Fork surfaces verified intact post-merge: `openai-completions.ts` keeps zero User-Agent references and the vLLM `created_cache_tokens` parsing beside upstream's empty-text-part filter (disjoint hunks, auto-merged); `generate-models.ts` keeps the Azure o-series xhigh pin beside upstream's `getModelsDevCost` extraction; `model-resolver.ts` keeps the fork's `cloudflare-ai-gateway` default while taking upstream's xai default; `stream.test.ts` keeps the `GATEWAY_KIMI` skipIf guards beside the grok-4.7 e2e block; `xai-responses.test.ts` keeps the zero-UA expectations (upstream's new grok-4.7 tests carry no User-Agent assertions). No manifest or lockfile changes upstream, so no install and no shrinkwrap/install-lock regen. Gates: `npm run check` exit 0; `./test.sh` all workspaces green. |
| 2026-09-21 | 36b60d2e8..4c8eb393c (48 commits, releases v0.86.0–v0.87.0: prompt cache warming #9668 with per-model `promptCache` tiers; canonical session context boundaries — SessionManager projections authoritative for provider requests, append-only `context_edit` entries, actionable turn boundaries via the new `finishTurn`/`prepareRequest` Agent hooks; transactional replicated chord state; compaction cancellation races #9340/#9777; image input limits #9631 with input-time `processImage`; bug reporting (local zip + opt-in Radius upload); Meta provider with Muse OAuth #9096; Radius model catalog; progressive session picker; Meta/z.ai/Cerebras/strict-mode provider fixes; WezTerm fullscreen images, LaTeX, CJK autocomplete, skill-autocomplete bare-name ranking #9120; crash logs; compiled Node CLI modules) | 41: 12 package.jsons (version-only — repo versions follow upstream, 0.87.0), 8 CHANGELOGs (fork-only [Unreleased] kept on top, upstream released sections verbatim, fork entries upstream has since released dropped), root lock + 4 example locks + shrinkwrap + install-lock (regenerated), agent-session.ts, compaction.ts, sdk.ts, session-manager.ts, settings-manager.ts, settings-selector.ts, interactive-mode.ts, tui/autocomplete.ts, docs/settings.md | The port obligations executed: upstream's canonical-projection types (UsageEntry, ContextEditEntry/ContextEditableContent, ProjectedSessionEntry/SessionProjection, standalone buildSessionProjection) moved from session-manager into `sessions/projector.ts` with the fork's extraction and re-export (row amended); `prepareCompaction` works over the canonical projection with the fork's replay ported onto it — `buildReplayMessages(checkpointMessages, …)` takes the checkpoint's projected messages, golden digests byte-identical (test file untouched); compaction/tree-nav rebuilds call `_refreshFinalizedContext` with the fork's prefix-monitor announcements beside it (2026-09-22 review follow-up: the refresh returns its projection so the token estimate does not rebuild it). Real deadlock fixed: upstream's deferred `agent_settled` actions ran inside the fork's `_settleWait` window, deadlocking a handler-started prompt — `_runDeferredSettledActions` now drains after the window closes (upstream's boundaries suite and the fork's model-handoff #122 both pin it). The shared afterToolCall hook adopts upstream's `inputLimits` resize; sdk's `buildRequestOptions` keeps the fork's opt-in attribution setting; `bug-report.ts` drops `pi-user-agent`/`trackingId` (row above). Fork tests adapted to upstream contracts: `context` event no longer carries system messages (`context_with_system` split), prompt images validated/resized at input (fake fixture → real 1×1 PNG), leading-slash skill completion matches bare names. Cache warming cannot pollute the prefix monitor by construction: warming goes through `modelRuntime.streamSimple`, never the observer-wrapped `agent.streamFunction`, and is gated to session-routed requests. Hazards hit: `npm install --package-lock-only` dropped cross-platform optional deps (@esbuild/aix-ppc64) breaking the shrinkwrap check — upstream's lock taken wholesale, shrinkwrap/install-lock regenerated from it; `meta.json`/`radius.json` are generator output that does not ride in the merge — `npm run generate-models` required; workspace `dist/` must be rebuilt (`npm run build`) or runtime resolves stale pre-merge Agent APIs (`peekQueuedMessages` missing). 2026-09-22 review follow-ups folded in: this sync row, the bug-report-upload traffic-audit row, and coding-agent [Unreleased] deduplicated against upstream's [0.86.0] (dropping #9068/#8964/#8133/#9294/#8967 that upstream formally released). Gates: `npm run check` exit 0; `./test.sh` all workspaces green; replay golden digests unchanged. |
| 2026-09-19 (second sync) | e4c75a732..36b60d2e8 (62 commits: the `pi-durable` package and durable conversation/task/document runtime with its harness/proxy work; forced-prompt projection replaces `SystemMessage.replace` — `transformContext` collapses system messages into a forced head without recording it; bash signal terminations report 128+signal exit codes #9577; mid-run threshold compaction skips oversized trailing tool results #9740; Bash/PowerShell minute-plus duration formatting #9628; llama.cpp `enable_thinking` respects Pi's thinking level #9528; Google resolver: lowest supported level when reasoning omitted #9455, gemini-3.8, mapless deepseek-v4 guard in generate-models; Kimi catalog source `kimi-for-coding`→`kimi-code-plan-global`; Cloudflare 520 #9627 and Azure capacity #9669 retry classification; README package table gains durable/telemetry rows; evals `autoevals` dep) | 9: README.md (fork's table kept, `pi-durable` row added; upstream's reordered chord/telemetry/ai/durable block dropped as duplicate), ai + coding-agent CHANGELOGs (union), google-thinking-level-map.test.ts (fork #124 wire tests + upstream #9455 `it.each` suite both kept; the exhaustive assertion aligned to fork #124 semantics — mapless xhigh/max degrade to high, only explicit garbage/null mappings throw), openai-completions-tool-choice + zai-coding-plan tests (upstream's — same provider restriction the fork made last sync, plus glm-5.3-highspeed zero-cost), agent-session.ts (observer wrap + upstream's `_installAgentForcedPromptProjection` both kept; the `extension-override` announcement moved from the now-deleted `updateMessage.replace` check to forced-prompt transitions inside the projection, tracked by `_lastForcedPrompt`), bash.ts (import union: fork `join` for artifacts spill + upstream `osConstants`), evals/package.json (fork 0.85.5 versions + upstream `autoevals`) | `SystemMessage.replace` is gone upstream: forced prompts now project per-request, so the fork's announcement hooks track entering/changing/leaving a forced prompt instead of the replacement message. Models: `models.generated.ts` unchanged upstream this range; `hydrate-model-data` re-run after the script's Kimi-source rename (ignored snapshots only). Deps synced (`npm install --ignore-scripts`, 28 added). Gates: `npm run check` exit 0; `./test.sh` — every suite passes, with three process-spawning tests flaking only under full-suite load on a loaded host (load average 10-15: `experimental-remote-runtime` activation deadline, `startup-session-name` CLI exit, `agent-session-concurrent` steer-while-streaming — the documented flake); each passes in isolation, the two coding-agent ones together with the rest of their files. |
| 2026-09-19 | 71dca871b..e4c75a732 (18 commits: mid-conversation system messages #9548 — the big one, system prompt and tool changes become transcript system messages instead of silent `state.systemPrompt` rewrites; `user_bash` fails closed #9662; Anthropic fallback model config #9294; Baseten session affinity #9629; OpenAI-compatible Responses error provider identification #9298; Bedrock 1h cache-write pricing #9457; clipboard backend failure surfacing + unverified local write rejection #9618; exact-ID session lookup without transcript scans #9601; extension event hook type exports #9642; documentation lift evals #9635; Google thinking levels from models.dev; image catalog refresh; forced system prompts persisted as replacing messages) | 13: ai/coding-agent/tui CHANGELOGs (union), anthropic-messages.ts (took upstream's rewritten adapter; re-purged the pi User-Agent — `mergeClientHeaders` dropped, calls go through `mergeHeaders`), openai-codex-responses.ts (upstream's transcript imports minus `getPiUserAgent`), image-models.generated.ts (theirs), google-raw-stop-reason.test.ts (ours — zero-UA expectations), agent-session.ts (upstream's `_preparePromptAndToolLoadout`/`NormalizedBuildSystemPromptOptions` architecture adopted; `_baseSystemPrompt`/`_systemPromptOverride` string fields retired; the request-observer hooks re-homed: tool-fingerprint announcement kept in `setActiveToolsByName`, `extension-override` announced only for replacing update messages, `session-reset` kept beside upstream's `_restoreToolsFromTranscript`), branch-summarization.ts + compaction.ts (fork replay kept; contexts wrapped via `normalizeContext`), session-manager.ts (fork's extracted structure kept; upstream's `findById`, compaction-entry `systemMessage` capture, and null-content system hardening auto-merged or ported into `sessions/projector.ts`), 2 compaction tests | Upstream's transcript architecture pursues the same cache-stability goals as the fork's monitor, so the port reuses it rather than forking the adapter: `serializeRequestPrefix` now derives its `systemPrompt`/`tools` sections from the transcript (`getCurrentSystemPrompt`/`getCurrentTools`) and drops the leading system message from the history section so divergence indexes still count conversation messages; `SummarizationPrefix` gained `systemMessage` — the replay prepends the transcript's own leading system message (byte-identical prefix, with a leading-duplicate dropped for checkpoint-headed transcripts) instead of re-deriving one from `systemPrompt`+`tools`. The fork's per-toolResult `tool_reference` wire machinery is superseded by upstream's in-transcript `toolsAdded` declarations: anthropic-wire-rewrite.test.ts deleted, the `onWireRewrite` options seam and observer wiring retained (attribution tests still pass through the injected streamFn; `provider-deferred-tool-load` now stays unused until an adapter needs it). system-prompt golden baselines regenerated to upstream's `<tools>`/`<rules>`/`<docs>`/`<skills>`/`<addendum>`/`<cwd>` section wrappers; `task` added to the declared-tools expectation (fork's default toolset). zai/glm catalog tests updated to the current models.dev snapshot (zai-coding-cn dropped glm-5.1/5v-turbo/5.2-highspeed; committed models.generated.ts identical to upstream — data hydrated with `hydrate-model-data`). Telemetry stays purged (no pi User-Agent anywhere). Gates: `npm run check` exit 0; `./test.sh` all 11 workspaces green. |
| 2026-09-12 | 6160683a4..71dca871b (36 commits: per-model compaction token budgets via `compaction.modelOverrides` #8133, agent retry backoff cap `retry.maxAgentDelayMs` #8826, extension tool parameter schema validation at registration #9300, OpenRouter session affinity headers on by default #9102 incl. `sessionAffinityFormat`/`x-session-id`, OpenCode `x-opencode-session` headers #9326, Codex Off reasoning effort #9191, Fireworks Messages deferred tool loading #9323 plus Fireworks thinking/effort catalog fixes, Mistral GLM-5.2 and Medium `reasoning_effort` #9375/#8700, DeepSeek Flash catalog #9423, retired GPT-5.4 Codex removal #9394, pico design docs under `packages/agent/docs/`, docs navigation validation #9380, interactive-testing/release skills extraction #9370, customization documentation evals #9491) | 3: AGENTS.md (adopted upstream's extract-to-skills structure — the tmux and Releasing bodies moved to `.pi/skills/` pointers; `interactive-testing.md` taken as-is, `release.md` rewritten for the fork's dispatch-only flow since upstream's verbatim skill instructs the npmjs tag flow this fork disabled), ai CHANGELOG (union: fork `cacheWriteReadRatio` + upstream Fireworks deferred-tools bullets both under Added; the coding-agent CHANGELOG auto-merged as the same union), anthropic-messages.ts (upstream's `sessionAffinityFormat` header-selection logic adopted; the call stays on the fork's `mergeHeaders` — `mergeClientHeaders` injects the purged pi User-Agent, so the affinity fix composes with the zero-attribution policy) | Upstream's own HEAD is red: 71dca871b ("fix(ci): Fix a broken test") deletes the `FinishReason.TOO_MANY_TOOL_CALLS` case from `mapStopReason` while the pinned `@google/genai` 2.21.0 (identical in both lockfiles) still carries the enum member — their push CI fails with the same TS2322 the fork hit (verified against the failed run's log); the fork restores the case as a deliberate divergence from a broken commit, to re-evaluate when upstream fixes their side or bumps genai. The per-model compaction budgets compose with the replaying summarizer without ledger-row changes — resolution happens in `getCompactionSettings(model)` before `prepareCompaction`, and the deferred-accessor decision row is unaffected (upstream's overrides are settings, not extension API). `marked` 18.0.11 added as a coding-agent devDependency (docs evals); node_modules hydrated, shrinkwrap + install-lock regenerated with no tracked diff (devDep, excluded from the published tree), `generate-models` re-run with no tracked churn (catalog data lives in git-ignored provider snapshots; upstream's rule changes rode in via the merged script). No README change upstream this range, nothing to port. Telemetry, version-check, and managed-install wiring stay dropped (purged files absent, zero references). Gates: `npm run check` exit 0; `./test.sh` all 11 workspaces, 2566 tests passed, one timeout in `agent-session-concurrent.test.ts` steer-while-streaming (the documented load flake again — load average ~8 during the run, passes in isolation). |
| 2026-09-08 | da840b621..6160683a4 (29 commits: runtime dependency updates #9341 incl. undici 8.10.2, upstream's session-fork streaming in `packages/agent` harness (dev-named-forks-streaming merge: JSONL migration/fork streaming, memory forks from live state, fork conformance suite), `ctx.modelRegistry.stream()`/`streamSimple()` extension streaming #8964/#9272, editor-border status spinners for compaction/branch/retry + operation UI preserved during tree navigation, tree navigation rejected during compaction #9179, strict-prefer JSON-schema sampling default for builtins, Radius model selection after catalog discovery, input handlers run for queued steer/followUp messages #8718, Copilot GPT models routed through Responses #9253, Mistral medium reasoning_effort #8700, EventStream drain quadratic-CPU fix #9055, empty-fullscreen-footer collapse #8919, tui clipboard simplification #9163 (drops `@mariozechner/clipboard`, adds native Linux clipboard), repository links pi-mono→pi #9278, documentation evals #9280) | 9: README.md (kept the fork's Releasing section; upstream's build-from-source-archive section NOT ported — fork releases attach no source archive, that text lives only in `build-binaries.yml`'s disabled flow), package-lock.json (regenerated), ai + coding-agent CHANGELOGs (union: upstream's [Unreleased] entries folded under the fork's; no version collision — upstream released nothing, fork stays 0.85.5), ai + protocol package.jsons (fork versions + upstream dep bumps), compaction.md (fork's summarizer-caching text kept, upstream's pi-mono→pi link rename adopted), install-lock lock + npm-shrinkwrap (regenerated) | Upstream's fork-streaming work is confined to `packages/agent/src/harness/session/` — no collision with the fork's `core/sessions/` modules, and coding-agent's session-manager.ts is untouched upstream this range. Dep updates adopted after review: undici 8.9.0→8.10.2 is a security-fix release (interceptor cache poisoning, BalancedPool TLS callbacks, WebSocket crashes, decompression bounds) plus h2/retry/no_proxy fixes — nothing affecting pi's fetch/ProxyAgent usage; chalk 6, typebox 1.3.27, google-genai 2.21.0, anthropic-sdk 0.124.0 ride along. Upstream trimmed the overrides to `protobufjs: 7.6.6` (rimraf/gaxios.rimraf pins dropped — deps updated past the bad ones); the fork's standalone packager carries overrides verbatim so it picks the trim up for free. Resolution hazard hit: `npm install --package-lock-only` kept protobufjs 7.6.5 locked despite the 7.6.6 override (npm does not re-resolve satisfied ranges on override change); `npm update protobufjs --package-lock-only` forced it, then node_modules hydrated and shrinkwrap/install-lock regenerated clean. `generate-models` re-run. Telemetry, version-check, and managed-install wiring stay dropped (purged files absent, zero references). Fork surfaces verified intact post-merge (capability + constrainedSampling union in tools, request-observer wiring in agent-session under upstream's `_runInputHandlers`/`_queueUserInput` refactor, APP_TITLE "Pi Fork", Radius `balanced` default adopted beside the fork's cloudflare default). Gates: `npm run check` exit 0; `./test.sh` all 11 workspaces ran, 2484 tests passed, one timeout in `agent-session-concurrent.test.ts` steer-while-streaming (the documented load flake, load average ~8 during the run; passes in isolation in 2.6 s). |
| 2026-09-05 (second sync) | 9841914c7..da840b621 (9 commits: Alt-wheel scroll acceleration #9166, source-only experimental exports #9132 incl. the `src/experimental/cli.ts` entry `pi-test.sh` now points at, remote-harness deps development-only, deterministic footer debounce tests, image-catalog refresh, Release v0.85.1, next-cycle [Unreleased]) | 35: 17 package.jsons + 7 lockfiles/shrinkwraps (kept at fork versions — upstream's v0.85.1 bumps only), 9 CHANGELOGs, README + environment-variables (kept fork telemetry-policy text; re-ported upstream's source-only-harness note for `PI_SERVER_DIR`/`PI_SERVER_ID`) | Second version collision, mirror image of 0.85.0: upstream released v0.85.1 while the fork already sits at v0.85.3, so the release entries do NOT union into one shared section — entries the fork already shipped in its own [0.85.1] (mouse hover, save keybindings #8797/#9149, GPT-6 Astra, GPT-5.6+ prompt-cache ttl) were unioned into the fork's [0.85.1], and entries new to the fork (Alt-wheel scrolling #9166, source-only experimental exports #9132) landed under [Unreleased] for the next fork release. ai's [0.85.1] was already byte-identical; the six other upstream [0.85.1] sections were empty. `models.generated.ts` identical on both sides. Adopted upstream's source-only exports structure in coding-agent/package.json (`./client` and `./experimental/plugin` source-only, `!dist/client|experimental|cli/experimental` in `files`, pi-client/protocol/server moved to devDependencies); root lockfile refreshed and shrinkwrap + install-lock regenerated (each drops the three moved packages). Resolution hazard hit and fixed: `git checkout --ours` on an unmerged path takes the whole stage-2 blob, discarding upstream hunks that had already auto-merged — coding-agent/package.json lost the exports/devDeps restructure (caught by the `package-distribution` and `experimental-remote-runtime` tests) and environment-variables.md lost the harness note; all 35 `--ours` files audited against upstream afterwards, only those two had lost content. Telemetry, version-check, and managed-install wiring stay dropped (purged files still absent, zero references). Gates green with true exit codes: `npm run check` + `./test.sh`. |
| 2026-09-05 | 17de82d7b..9841914c7 (1 commit: tui list selection unchanged on mouse hover) | none | First sync after the 2026-09-05 history rebuild (fork main rewritten as 8 thematic squash commits directly on upstream/main, tree byte-identical, old history kept on `backup-main`): the merge applies cleanly on the rebuilt history. Telemetry, version-check, and managed-install wiring stay dropped (all four purged files still absent, zero references). |
| 2026-09-04 (third sync) | dd7e816b5..17de82d7b (3 commits: GPT-6 Astra support, selector save keybindings #9149/#8797, Docker Sandboxes docs #9077) | 2: `packages/ai/scripts/generate-models.ts` (union — the fork's Azure o-series xhigh pin and upstream's new gpt-6-astra map are independent rules at the same insertion point; both kept), coding-agent CHANGELOG (union — fork [Unreleased] entries kept, upstream's keybindings Fixed entry added under Fixed) | Small sync. Telemetry, version-check, and managed-install wiring stay dropped (re-verified: all four purged files still absent, zero references). `generate-models` re-run after the merge (Astra catalog churn). No lockfile/manifest churn, no npm install needed. Fork surfaces verified intact post-merge (capability/timeoutMs fields, request-observer, both builtins, tps extension). Gates green: `npm run check` + `./test.sh` (exit 0). |
| 2026-09-04 (second sync) | 6aedd1066..dd7e816b5 (7 commits: upstream's v0.85.0 release — Grok Build 0.1 catalog removal #9093, fullscreen search scaling #8800, upstream's own Copilot catalog-churn test fixes, changelog audit, Release v0.85.0) | 5: three Copilot/codex catalog tests (took upstream's — they independently fixed the same model-id churn the fork had fixed with different ids; the fork's local pins are superseded), ai + coding-agent CHANGELOGs | First version collision: both sides released "0.85.0 - 2026-09-04" (the fork's first `release:minor` cycle). Resolution recorded as convention: the fork's [0.85.0] section is the union — fork entries kept in place, upstream-only entries (16: uuidv7 timestamp arg, assistant-message frames, subpath exports, vllmPriority/#8941, NO_PROXY, Baseten/Fireworks, session shares/forks/imports fixes, ctx.cwd, EXIF, SIGWINCH, LaTeX, search scaling, musl fd/rg, skills-with-bash, in-memory restore) inserted under matching subsections, since the 0.85.0-fork.6 tarball shipped both change sets; dropping either side would misdocument what fork users actually run. Upstream's tag `v0.85.0` fetched cleanly — the earlier deletion of the fork's stray bare `v0.85.0` tag is what avoided the collision. `npm --prefix packages/ai run generate-models` re-run for the new xAI exclusion (stale `providers/data` kept `grok-build-0.1` alive in `xai.models.ts` and failed the exclusion test). Gates green: `npm run check` + `./test.sh` (exit 0). |
| 2026-09-04 | 1d9787c11..6aedd1066 (4 commits: tui alt-mode scrollbar #8801, fullscreen jump-to-end indicator #9080, agent signal-killed exit codes #8994, musl fd/rg builds on Linux #9070) | coding-agent CHANGELOG only (union: upstream's jump-to-end entry folded into the fork's Added list) | All four adopted; the jump-to-end indicator and alt-mode scrollbar compose with the fork's fullscreen chat viewport. Gate note: `agent-session-concurrent.test.ts`'s followUp-while-streaming test (upstream-owned, timer-driven) times out at its 30s limit when machine load sits near saturation (~11 on 12 cores) under `./test.sh` — it passes in isolation and at normal load; treated as a load flake, not modified. Gates green: `npm run check` + `./test.sh` (exit 0). |
| 2026-09-03 (second sync) | e44d75c20..1d9787c11 (1 commit: tui Working... spinner #8799) | coding-agent CHANGELOG only (union: fork [Unreleased] entries + upstream's spinner entry) | Trivial sync that exposed hidden fallout from the earlier big sync: the merged coding-agent vitest.config.ts had lost the fork's `fileURLToPath` import, so the whole coding-agent suite failed at config load — earlier gate runs piped output through `tail`/`grep` and lost the nonzero exit. Repairs shipped alongside this sync: vitest config import restored; tool-previews passes real definitions (`createAllToolDefinitions`) because upstream's `ToolExecutionComponent` no longer looks up built-in renderers itself; system-prompt golden updated to upstream #8552 (skills section stays with bash-only tools, loaded via bash); the #56 wire-rewrite mock implements `client.beta.messages` (upstream's adapter moved off plain `messages.create`); remote-runtime idle-worker test polls for OS process exit. Also documented hazard: subpaths the vitest aliases do not cover (`@earendil-works/pi-ai/utils/uuid`, chord runtime imports through node) resolve to workspace `dist/` builds — upstream CI runs `npm run build` before tests, so after a sync the per-package tsgo dists must be re-emitted (chord, ai, agent, protocol, client, server, telemetry) or timestamp/uuid assertions fail against stale artifacts. Gates green with true exit codes: `npm run check` + `./test.sh`. |
| 2026-09-03 | 853a80d26..e44d75c20 (435 commits, all unreleased upstream work — packages stay 0.84.4) | 29: agent-session.ts, main.ts, anthropic-messages.ts, interactive-mode.ts, assistant-message.ts (+ its test), branch-summarization.ts (+ its test), grep.ts, read.ts, vitest.config.ts, system-prompt.test.ts, agent-session-compaction.test.ts, README.md + 3 docs, 4 CHANGELOGs, 4 package.jsons (took theirs), .gitignore, 3 lockfiles (regenerated) | Largest sync to date. Adopted: upstream's Anthropic adapter restructure (`convertMessages` returns `ConvertedAnthropicMessages`, `managedProvider` param, `insertThinkingLevelMessages` per-turn thinking effort for `supportsMidConvoEffort` models) with the fork's `onWireRewrite` threading and both fire sites composed into the new signature (row below updated); the renderer-extraction architecture (`core/tools/renderers/` + `withBuiltInRenderers`) and `createChatViewport` fullscreen chat layout — read's counts-first delta re-ported into `renderers/read.ts` while `read.ts` itself is upstream's plus the one-line `capability` field, and grep keeps its fork renderers inline on the definition, which wins over the built-in fallback (counts-first row updated); #8845 branch-summary `maxTokens` clamp derived from the model, inside the fork's prefix-replaying request (clamp test re-added with the required `prefix`); theme JSON validator wiring in main.ts; vitest.config alias union. Not ported: upstream's per-run thinking click-toggle (`MouseRegion` + `thinkingVisibilityOverrides` in assistant-message.ts and its test) — the fork's thinking preview (live timer header, fade, fixed-height block) replaces it. agent-session.ts event union keeps both sides (fork `auto_retry_end`/`prefix_invalidated` + upstream `bash_execution_update`). Telemetry, version-check, and managed-install wiring stay dropped (re-verified). Lockfiles regenerated (root `--package-lock-only`, install-lock + shrinkwrap generators); `@anthropic-ai/sdk` bumped by upstream to 0.123.0 — node_modules needs a real `npm install --ignore-scripts` after this sync, and `npm --prefix packages/ai run generate-models` must run before check/tests (openrouter now serves `anthropic-messages` models; without fresh `providers/data` the provider's api-map type narrows and check fails). Gates: `npm run check` passed; the `./test.sh` run reported green but was masking a failure — the coding-agent vitest config lost its `fileURLToPath` import in the merge, so that suite failed at config load and piped output hid the exit code (repaired with the next sync, 2026-09-03 second row). |
| 2026-08-29 | 6c87d9a02..853a80d26 (4 commits: v0.84.4 release, image-catalog refresh, changelog audit) | 2 CHANGELOGs | Release sync: version bumps to 0.84.4 across all packages and the models.generated.ts image-catalog refresh taken as-is. Changelog conflicts resolved by keeping fork-authored entries in [Unreleased] and dropping the inherited-marked ones that upstream formally released into its [0.84.4] section (they ride into fork releases via synced upstream sections now, matching how 0.84.3 entries behave); upstream's section lands below [Unreleased] in both files. No telemetry or managed-install re-additions; check + full test.sh green. |
| 2026-08-28 | 4e4949299..6c87d9a02 (5 commits) | agent-session.ts (both sides added methods at the same spot — kept both: fork `_evaluateToolPermission` + upstream `_compactBeforeNextAssistantResponse`), agent-session-compaction.test.ts (imports: fork `Message` + upstream `fauxToolCall`), 2 CHANGELOGs | Upstream #6879/#8782 compacts between tool execution and the next assistant response; it calls the fork-wired `_runAutoCompaction`, so the announce + request-kind windows apply to the new caller automatically. Upstream #6879 also narrows `prepareNextTurn` to turns that continue (agent CHANGELOG breaking change adopted as inherited). Fireworks turbo-router test removed upstream — that catalog drift is what reddened PR #91's CI (the PR's own content was green); this sync fixes forward. Mistral indexed tool-call chunk merge adopted. No telemetry or managed-install re-additions. |
| 2026-08-27 | e86823096..4e4949299 (3 commits) | coding-agent CHANGELOG only | Additive sync: `fullscreenCopyOnSelect` + Ctrl+X selection copy (#7720/#8731), `ui_prompt_start`/`ui_prompt_end` extension events (#5329/#8355), Google Vertex https-proxy-agent named-export fix (#8610/#8723). No telemetry or managed-install subsystem re-additions to re-drop. CHANGELOG conflict resolved by keeping the fork's [Unreleased] entries and adding upstream's three new ones marked inherited; upstream's re-listing of the already-released transcript-notices entry skipped. Fork surfaces verified intact post-merge (permissions settings, request-observer, ToolInfo capability + timeoutMs, telemetry removals). |
| 2026-08-26 | 8fa7eebd2..e86823096 (11 commits) | compaction.ts, settings-manager.ts, 3 tests, CHANGELOG | Upstream removed `toolChoice: "none"` from summarizer calls (#8649/#8638); adopted in both `cacheOptOut` branches (persona instruction + rejects-tool-call handling already cover it). e2e cache test classification reworked to the persona marker. Upstream's session-tail newline repair (0b5ee5d8b) is complementary to the fork's read-only validator. Settings test needed both sides' imports. |
| 2026-08-24 | 460191cfc..a470b121b (4 commits) | `agent-session.ts` (import trim vs fork's `join` usage; resolved by taking upstream imports + re-adding `join`) | README untouched upstream, no port needed; ledger assumptions re-verified; fork test set green |
| 2026-08-24 | a470b121b..dcd461925 (8 commits, v0.84.3) | `tools/bash.ts` (upstream extracted a shared `createShellToolDefinition`; fork's `capability: "process.execute"` moved onto the shared factory, covering powershell too), `tools/index.ts` (fork permissions exports + upstream powershell exports, both kept), `package-manager-cli.ts` + `test/package-command-paths.test.ts` (managed-install subsystem dropped per the decision above; fork self-update flow kept), `main.ts` (`cleanupManagedInstall` wiring unwound) | Upstream's installer-managed update subsystem not ported (see Decisions); `docs/packages.md` managed sentence reverted to pre-upstream text; `minimal` profile deny+hide list gains `powershell` (registered by default via `createAllToolDefinitions`); powershell ships with `process.execute` capability; golden system-prompt and prompt-prefix tests green without updates; fresh worktrees must run `npm run generate-models` (in `packages/ai`) before `npm run check`/tests — `src/providers/data/.manifest.json` is a git-ignored artifact whose absence cascades `unknown`/`never` type errors |
| 2026-08-25 | dcd461925..8fa7eebd2 (16 commits) | `cloudflare-ai-gateway.ts` (upstream adopted the fork's pinned generic as the named `CloudflareAIGatewayApi`; took upstream's shape, kept the fork's explanatory comment), `zai-coding-plan-models.test.ts` (fork and upstream pinned identical glm-5.3 prices; took upstream's two-provider loop, dropping the fork's single-provider duplicate), `coding-agent CHANGELOG [Unreleased]` (fork #40/#41 entries combined with upstream #8432/#8537 marked inherited) | README untouched upstream, no port needed; upstream's custom-message deferral to `turn_end` (#8537) composes with the prefix monitor (deferred messages append after the turn's tool results, so the request prefix stays append-only); RPC `clear_queue` (#8432) only exposes the pre-existing `AgentSession.clearQueue()` over RPC; `tool_choice`-without-tools fix (#8607) is what the replaying compaction summarizer needs (`toolChoice: "none"` with the full tool list); glm-5.3 price test needs `providers/data` regenerated after the merge (`npm run generate:models` — the z.ai price change postdates the previous sync's data) |

## Upstream Sync Procedure

1. Merge upstream `main` into fork `main` as a dedicated `upstream-sync:` commit; never mix fork feature changes into a sync commit. Push the sync to `main` directly — never through a PR: the repo allows squash merges only, and a squashed sync PR would drop the upstream merge parents, resetting the merge-base for every later sync.
2. Rebase or merge stacked fork branches onto the updated fork `main` afterwards.
3. Run `npm run check` and `./test.sh` from the repo root; fix fallout before continuing.
4. If a sync invalidated an assumption recorded in the table above, update that row and the associated test in the same change.

Tag namespace: fork releases live only in the `v<version>-fork.N` namespace, created by the manual Fork Release workflow (GitHub Packages + GitHub Release with `pi-fork.tgz`); nothing auto-publishes on a tag push, and bare `vX.Y.Z` tags must never be pushed to this repo — upstream tags the same names, so a fork `vX.Y.Z` collides on the next `git fetch upstream --tags`. The 2026-09-04 `release:minor` run (inherited upstream script) pushed a bare `v0.85.0` before this was noticed; the tag was deleted immediately (the `Release v0.85.0` and `Add [Unreleased] section` commits on `main` remain valid). The version bump and changelog fold from that run are the fork's inputs to the Fork Release workflow, which derives `0.85.0-fork.<run>` from the package version. The same 2026-09-04 round caught the first standalone-tarball regression from the chord adoption: `v0.85.0-fork.5` shipped without chord (the bundler keeps it external; the packager filtered it as a workspace dep), so its `pi` crashed at startup — the release and tag were deleted, and `fork-standalone-package.mjs` now vendors chord as a bundled dependency with its pinned transitive hoisted (`v0.85.0-fork.6` is good; verified by installing the released tarball and running a live prompt).
